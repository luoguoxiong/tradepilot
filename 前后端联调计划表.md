# TradePilot AI · 前后端联调计划表

> 对象：**Web 前端（`web/`）** ↔ 后端真实接口（`server/`），覆盖 P0 全量模块。

| 项         | 内容                                                                                                                                                                                                                                                                                                     |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 版本       | v0.1（2026-09-09）                                                                                                                                                                                                                                                                                        |
| 依据       | [前端开发排期计划](./前端开发排期计划.md) · [后端开发计划表](./后端开发计划表.md) · [页面级字段与接口文档 00~16](./产品需求文档/页面级字段与接口文档/00-总览与接口规范.md)                                                                                                                                  |
| 范围       | P0 全量：auth/初始化向导/16 设置 P0/权限矩阵/异步任务 SSE/03 获客/02 员工中心/05 CRM/04 客户 360°/06 工作台/12 审核中心/07 自动跟进/11 知识中心/01 Dashboard + 端到端回归                                                                                                                                    |
| 前端基线   | M1~M6 已完成（mock 口径 100%，MSW 契约测试 37+ 例，门禁 lint/type/test/build/size 全绿）                                                                                                                                                                                                                 |
| 后端基线   | M1~M3 + M4 主链完成（11/12 接口已交付）；M5 提前至 W2（09-14）启动，目标 **W8（10-30）前收口**；Gmail/Outlook 真实收发冒烟仅剩 B1 OAuth 审核（不占主线）                                                                                                                                                   |
| 切换方式   | 真实环境：`VITE_USE_MOCK=false`；`/api` 已由 Vite proxy 转发到 `VITE_PROXY_TARGET`（默认 `http://localhost:8080`），SSE 代理已关响应缓冲（`vite.config.ts` configure）                                                                                                                                    |
| 联调判定   | 关闭 mock 后请求打到真实后端并返回符合接口文档 envelope 的数据；关键链路（SSE/审批/上传/权限）端到端跑通即为「完成」，否则如实标「阻塞」原因                                                                                              |
| 状态图例   | ☐ 未开始 · 🔄 进行中 · ⛔ 阻塞（注明原因） · ✅ 已完成（后端真实接口验证通过）                                                                                                                                                                                                                             |
| 时间口径   | 「目标窗口」= 对应后端模块交付后 1~2 周内启动，后端提前就绪则**随到随联、不必等窗口**；中秋（09-25）与国庆落在 W3~W5，按实际放假整体顺延。**「是否完成」列在表末，以真实接口跑通为准。**                                                                                                                     |

---

## 1. 联调计划总览（主表）

| #  | 联调单元（前端页面 / 入口）                                                            | 联调范围与关键通过标准                                                                                                                                                                                                 | 后端就绪依赖（接口组）                                                                      | 目标窗口（2026）              | 前端 / 后端主责 | 是否完成 |
| -- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------- | --------------- | -------- |
| 1  | 通道与基础（前置，无页面）                                                              | `VITE_USE_MOCK=false` 全量切真；统一 envelope / 分页 / 金额字段解析；错误码管道 40001~50301 各 UI 行为（40101 跳登录、40301 越权空态、40901 冲突回滚、42901/50301 提示）；axios 与 fetch-event-source 均带 Bearer                                                                                     | auth（M2 已完成）+ 代理/环境（M1 已完成）                                                     | W1（09-09 起）               | FE-A / BE-C     | ✅        |
| 2  | 登录 / 注册 / 企业初始化                                                                 | `LoginView`/`RegisterView`（防枚举提示、密码≥8）/ `OnboardingView` 四步向导断点续走（onboarding.currentStep）；注册→登录→`/api/org` me → 进入主框架                                                                            | auth / org（M2 已完成）                                                                       | W1~W2（09-10 ~ 09-18）        | FE-B / BE-A     | ✅        |
| 3  | 系统设置 P0（`settings` 6 子页）                                                       | `OrgInfo` 本地化三件套时区回显；`Members` 成员/邀请；`Mailboxes` IMAP/SMTP 配置 + **测试连接**；`ApprovalRules` autoApprove + policy；`Notifications` 通知规则；`AiEmployeesEntry` 入口                                                                                                                    | settings / members / mailboxes（M2/M4 已完成）                                               | W2（09-14 ~ 09-18）           | FE-B / BE-C     | ✅        |
| 4  | 全局框架与权限矩阵                                                                      | `DefaultLayout` 菜单按角色渲染、路由守卫（认证/初始化/角色/标题时区）；三角色（admin/manager/sales）scope=self/team/all 数据裁剪；`notifyStore` 待审数 15s 轮询（页面隐藏暂停）对端待审聚合                                                                                                                  | org + RBAC（M2 已完成）+ notifications 待审聚合（M5-A2，W2~W3）                              | W2~W3（09-14 ~ 09-25）        | FE-B / BE-C     | ✅        |
| 5  | 异步任务与 SSE 通道（共用基座）                                                         | 真实 `POST /tasks` 创建（7 类 task_type）；SSE `/stream` 真实事件流（fetch-event-source 带 Bearer、代理关缓冲验证）；断线后 `/logs?after=` 增量补齐与降级轮询；steps/retry 溯源；TaskProgress 终态（done/failed）刷新                                                                                                                 | tasks + SSE/logs（M3 已完成）                                                                 | W2（09-14 ~ 09-18）           | FE-A / BE-A     | ✅        |
| 6  | 11 知识中心（`knowledge`）                                                             | 批量上传→真实分块/嵌入索引→3s 轮询状态列→可检索；`knowledge_search` citations 引用链回显（标题/分类/来源）；删除留痕 / retry；manager 权限隔离                                                                                                        | knowledge（M4-C5/C4 已交付）                                                                  | W3~W4（09-21 ~ 10-02）        | FE-B / BE-B     | ✅        |
| 7  | 05 CRM（`crm` + `crm/contacts`）                                                      | 四页签列表（tab/keyword/scope）+ 筛选分页；新增/编辑表单；阶段机流转（非法 40901 提示）；联系人 CRUD + 唯一 40901；**软删/批量删除走 12 审批（deleteLocked 联动、拒绝自动解锁）**；批量改派（仅 manager、越权 40301）+ owner_change 留痕；AI 建议列                                                                  | customers / contacts / activities（M5-A4 + B1，W2~W6 分批）                                  | W3~W5（09-21 ~ 10-09）        | FE-A / BE-B     | ✅        |
| 8  | 03 AI 获客（`lead-gen`）                                                              | 自然语言创建→`parse` 结构化回填（confidence/reasons）→创建 lead_hunting 任务→SSE 日志流实时滚动→done 刷新；发现列表评分分档（InsightCard hover）；**批量加入 CRM**（域名三级去重、duplicate 分流、mapped 不归属）；已转化行禁用                                                                                | leads / lead-tasks（M5-B2，W3~W6 交付）                                                        | W5~W6（10-05 ~ 10-16）        | FE-A / BE-B     | ✅        |
| 9  | 02 AI 员工中心（`ai-employees`）                                                      | 六卡片墙状态渲染（working/done 归位、当前任务 currentTask）；分步创建向导（sopParams 派生、审批策略绑定、kpiConfig 校验）；暂停/恢复；任务详情抽屉复用日志流；D4/D5 降级占位核对                                                                                                  | ai-employees / roles / tasks（M5-C3，W2~W6）                                                 | W5（10-05 ~ 10-09）           | FE-B / BE-A     | ✅        |
| 10 | 04 客户 360°（`customers/:id`）                                                       | 头部聚合（阶段/语境化动作）+ 8 页签懒加载（D6/D7 隐藏降级）；活动时间线分页；AI Insights 证据链（confidence/reasons/citations + estimated 角标）；**analyze 触发异步任务→重新分析**；generate-outreach 仅产草稿 + 自动建 outbound 会话                                                            | customers `{id}` 子资源 / insights / conversations（M5-B3，W3~W6）                            | W5~W6（10-05 ~ 10-16）        | FE-B / BE-B     | ✅        |
| 11 | 06 销售工作台（`inbox`）                                                             | 会话双层列表（多邮箱来源/未读/筛选）；DraftEditor 真实草稿链路：ai-draft / regenerate（retry_of 溯源）→ insert_draft 一键填入（citations 依据区、grounded=false 兜底、语言检测）；**send 联动审批**（分支 A 直发 / 分支 B 生成审批单）；消息编辑差异留痕；ask-ai（会话+知识 RAG）；邮件原文沙箱预览                                 | conversations / ai-draft / send / messages / ask-ai（M5-A3/C1/C2，W2~W6）                    | W6~W7（10-12 ~ 10-23）        | FE-A / BE-A     | ☐        |
| 12 | 12 AI 审核中心（`approvals`）                                                        | 待审摘要/列表（按来源 Tab 渲染：email_send / customer_delete）；**approve / reject（必填理由）→ 任务/客户级联**；编辑后批准跳 DraftEditor；超时（expires_at）终态禁用、乐观移除；审批后通知角标联动（#4 轮询口径对端一致）                                                                                          | approvals（M4-C7 已交付，数据通路依赖 06 send/05 软删）+ notifications                        | W6~W7（10-12 ~ 10-23）        | FE-B / BE-B     | ☐        |
| 13 | 07 AI 自动跟进（`follow-up` + `follow-up/strategies`）                               | 策略表单 CRUD（默认策略禁删禁改、编辑=复制、dayOffset 递增唯一 42201、breakup 强制人工）；**apply 单人/批量**（每客户仅 1 进行中任务、next_run_at 企业时区）；任务列表状态 Tab（skipped 原因留痕）；executions / pause / skip；customer_replied 自动暂停联动                                                                                 | follow-ups / strategies / executions（M5-D1，W6~W7）                                          | W7~W8（10-19 ~ 10-30）        | FE-A / BE-B     | ☐        |
| 14 | 01 Dashboard（`dashboard`）                                                         | 聚合接口按字段存在性渲染（D1：未启用 metric 不返回而非 0）；KPI 自适应；今日待处理跳转预置筛选（D2 当地日历日口径）；onlineEmployeeCount 口径核对；daily-report 恒 40401（D3）降级；全量 D1~D12 后端口径逐项走查（后端 A1 判定器为唯一事实源）                                                            | dashboard summary（M5-D2 + A1 判定器，W6~W7）                                                | W8（10-26 ~ 10-30）           | FE-B / BE-C     | ☐        |
| 15 | 端到端主链路回归（跨模块）                                                           | staging 部署后真实环境全量回归六大主流程：①登录初始化 ②获客→转 CRM ③发送→待审→编辑后批准 ④审批三态 ⑤CRM 批量/权限 ⑥权限裁剪三角色；弱网/断线（SSE 降级）演练；P0 验收 FR 清单 + UAT、bug fix                                                                                                          | staging 全量（后端 M6 部署，W12~W13 预联调）+ 前端 M7（W13）                                   | W13~W15（11-30 ~ 12-18）      | 双人 / 三人     | ☐        |

> 说明：
>
> - **已具备即期联调条件**的项（#1~#5）后端均已交付，无需等 M5，前端可在 W1~W2 直接切真联调，尽早暴露鉴权/SSE/错误码三类基础问题。
> - #7~#10 依赖 M5 客户域（A4/B1/B2/B3，W2~W6 分批交付），随交付进度分批联调，不必等整行后端全部完成；模块内多个子项可拆粒度为「联调子项」在会议纪要/缺陷单中跟踪。
> - 12 审核中心接口已交付但**需 06 send / 05 软删 产生真实审批单**后才有联调数据，#12 因此排在 W6 后，与 #7/#11 串行衔接。
> - Gmail/Outlook 真实收发冒烟依赖 B1 OAuth 审核通过（后端排期风险 #2），若在 12-04 前未过审，以 SMTP-IMAP 驱动兜底验收，并如实标记 #11/#15 的阻塞面。

---

## 2. 就绪依赖速查（对齐后端计划 §4 协同窗口）

| 接口组（真实可联调批次）          | 前端消费方                 | 后端就绪时点（计划）                 | 备注                                             |
| -------------------------------- | -------------------------- | ------------------------------------ | ------------------------------------------------ |
| auth / org / 成员 / 16 设置 P0    | #2 #3 #4                   | W2（09-14 前后，M2/M4 已交付）       | 即期可联                                         |
| tasks 基础 + SSE + logs 增量      | #5                         | M3 已完成（09-08）                   | 即期可联                                         |
| 11 知识中心（上传/检索/审核）     | #6                         | M4 已完成（09-09）                   | 即期可联（需真实嵌入服务可用）                   |
| 05 CRM + 联系人 + 批量/审批软删   | #7                         | M5-A4/B1（W2~W6）                    | 与 #12 审批数据通路联动                          |
| 03 获客 + leads + add-to-crm      | #8                         | M5-B2（W3~W6）                       | 获客全流程                                       |
| 02 员工中心（CRUD/currentTask）   | #9                         | M5-C3（W2~W6）                       | 依赖 ai_employee/sop 模板（已就绪）              |
| 04 客户 360° 聚合与子资源         | #10                        | M5-B3（W3~W6）                       | 页签/降级口径需 A1 判定器                         |
| 06 销售工作台写侧 + send 审批联动 | #11                        | M5-A3/C1/C2（W2~W6）                 | send 双分支依赖 Approval Gate（M3 已就绪）       |
| 12 审核中心数据通路               | #12                        | M4-C7 已交付，通路 W6 后              | 靠 06/05 产单                                   |
| 07 自动跟进 + 策略 apply          | #13                        | M5-D1（W6~W7）                       | 频控/时区窗口口径（computeDeferredNextRunAt）    |
| 01 Dashboard 聚合（D1/D2/D3）     | #14                        | M5-D2 + A1（W6~W7）                  | D1~D12 降级口径核对                              |
| staging 全量环境                  | #15                        | W13 末（12-04）                      | 12-07 起全量联调与 UAT                           |

---

## 3. 联调执行约定

1. **切真窗口开关**：单模块联调开始时按模块在本地 `VITE_USE_MOCK=false` + proxy 指向 staging/本地后端；未联调模块继续走 mock，避免一次全量切换阻塞多路并行。
2. **契约护栏**：后端接口与接口文档不一致时，遵循「mock 即契约」——先在 MSW handler/契约测试（`web/src/mocks`、`tests/contract`）上暴露漂移，再与后端确认文档先行、同步前端类型（`web/src/api/types`）。
3. **记录与闭环**：每行联调产出在对应 PR 描述或缺陷单登记；阻塞项在表末「是否完成」标 ⛔ 并注明阻塞点与负责人，解阻后置 ✅。
4. **双周节奏**：每双周五复盘一次本表，更新状态列并同步修订两个排期文档；联调不占用里程碑出口判定。
5. **已知风险**：真实 SSE 流、审批新鲜度校验、时区窗口、Gmail OAuth 审核周期为本计划最大变量，遇阻优先按后端排期 §5 对策兜底。

---

## 4. 变更记录

| 版本 | 日期       | 说明                                                             |
| ---- | ---------- | ---------------------------------------------------------------- |
| v0.1 | 2026-09-09 | 首版：对齐前端 M1~M6 交付基线 + 后端 M5 提前窗口输出 P0 联调计划 |
| v0.1.1 | 2026-09-09 | #2 登录/注册/初始化、#3 系统设置 P0 6 子页：真实后端（VITE_USE_MOCK=false → :8080）E2E 走查通过并修复 RLS 登录缺陷 + i18n `@` 占位符编译问题，状态置 ✅ |
| v0.1.2 | 2026-09-10 | #4 全局框架与权限矩阵：三角色真实环境走查通过置 ✅。① scope 接口级矩阵 12/12（sales scope=team/all→40301、缺省=self 仅本人；manager scope=all→40301、缺省=team=org 全员；admin 缺省=all）+ UI 三角色验证（sales 不渲染 ScopeSelect 且默认仅本人数据、manager 默认 team 且选项不含全部、admin 默认 all，切换即时裁剪）；② notifyStore 15s 轮询真实环境存活 + 徽标联动，隐藏暂停/恢复补刷以假时钟单测 `tests/unit/polling.spec.ts` 确定性覆盖（真实浏览器 60s 间隔系 Chrome 对遮挡窗口的计时器节流副作用，非缺陷）；③ 菜单/守卫：菜单由路由表自动生成（02 §1.1）+ 页面级 v-permission 裁剪（sales 邀请成员/审批处置按钮均隐藏）符合 05 §3.2，认证守卫/标题时区 ✅，角色守卫机制就位（当前 P0 路由无 roles 声明）。联调修复 3 缺陷：#1 审核中心「全部」Tab 误传 type=all → 40001（ApprovalsView 不传 type + mock 收紧）；#2 前端 PageResp.list → items 对齐接口规范 §2.2（类型+mock+19 处消费点）；#3 ProTable scope 默认值硬编码 self → 取 usePermission().maxScope 角色上限。回归：vue-tsc ✅、vitest 20 文件 158 用例全通过 |
| v0.1.3 | 2026-09-10 | #5 异步任务与 SSE 通道（共用基座）：真实环境（API :8080 + Worker Node20 + PG/Redis）走查通过置 ✅。① `POST /tasks` 7 类 task_type 真实创建并入队/排队/定时判定正确；② SSE `/stream` 真实事件流跑通（`fetch-event-source` 带 Bearer、响应头 `text/event-stream` + `X-Accel-Buffering: no`，事件 `log`/`progress`/`status`/`done` 契约正确，终态 `completed`）；③ 断线后 `/logs?after=` 增量补齐正确（`hasMore`/游标一致），降级轮询由 `task-stream` 集成测试（兜底轮询用例）确定性覆盖；④ `steps` 与 `retry` 溯源（`retryOf` + input 复制）通过；⑤ `TaskProgress` 终态（done/failed）刷新由 `status`+`done` 事件驱动。联调修复 2 后端缺陷：**#1 SSE 实时 `log` 事件缺 `time` 字段**（与 `/logs` 单条、前端 `TaskLog` 契约不一致，日志时间列渲染异常）——在 `sseLogPayloadSchema` 增补 `time` 并在全部 4 处日志源（`writeToolLog`/`compiler.completeStep`/`approval-gate`/`flows.finalize`）以 `occurredAt` 同源写入，`buildLogEvent` 兜底；**#2 `lead_hunting` finalize 日志只发 SSE 不落库**（断线补拉永久丢失，SSE 55 vs `/logs` 54）——改为事务内先写 `ai_task_log` 再推事件。复验：修复后 SSE 55/55 `log` 携带 `time` 且与 DB `occurred_at` 逐毫秒一致，`/logs` 55 条含 finalize，**实时与回放完全同构**。同步修订接口文档 14 §3.4（SSE `log.data` 补 `time` 并注明与 §3.3 同源同构）。回归：后端 `turbo typecheck` 16/16 ✅、`tasks`/`task-stream` 集成测试全通过（其余失败项为本地缺 `notification` 表/列等既有环境缺口，与本项无关） |
| v0.1.4 | 2026-09-10 | #6 11 知识中心：真实环境（API :8080 Node20 单实例 + Worker + PG/Redis，嵌入用本地 Mock provider）走查通过置 ✅。① 上传白名单（md/txt/pdf/docx）成功→Worker 真实分块/嵌入索引，列表 3s 轮询 `indexing→indexed` 自动停；伪装扩展名 / `.exe`→42201、51MB 超限→42201；批量并发上传 2 文件均入队；② `knowledge_search` 命中返回 citations（`docName/docId/chunkId/score`）、无关 query `noResult=true`、软删后不再召回；③ 详情（引用解析）`updatedBy` 为姓名；软删留痕（列表过滤、详情仍可追溯 `deleted/deletedBy/deletedAt`）、重复删除 40901、retry 仅 failed 可重试（否则 40901）；④ stats `documentsCount/chunksCount/lastIndexedAt` 真实；⑤ 权限隔离：sales 上传/列表/检索允许、delete/retry→40301，manager delete 允许。联调修复 3 缺陷：**#1 「全部」Tab 误传 `category=all`→40001**（KnowledgeView 对 `all` 不传 category，mock 收紧为枚举校验，契约测试补 `category=all→40001`）；**#2 列表响应与接口文档/前端列不一致**——`size` 返回原始字节数（应为可读字符串）、字段名为 `uploadedBy`（原始 userId）而非 `updatedBy`（上传人姓名，与 detail §3.5 及前端 `colUpdatedBy` 一致）——后端 `list` 改为 `leftJoin user_account` 回传 `updatedBy` 姓名 + `size` 格式化；**#3 向量召回无阈值致 `noResult` 永不成立**（任意无关 query 都返回近邻 Top-20，违反 11 §1.3 FR-06「无相关信息须明示、禁止编造」）——检索向量路加余弦距离阈值（`< 0.75`），Mock 近正交嵌入被自然滤除、真实嵌入相关文本仍保留召回。回归：前端 `vue-tsc` ✅、vitest 20 文件 159 用例全通过；后端 `typecheck` 16/16 ✅、知识集成测试 12/12 通过（复跑需隔离 Redis DB 或以 `REDIS_URL=.../5` 排除本机常驻 Worker 抢消费队列；另清理本机 7 个重复 `api dev` watcher 并恢复 Node20 单实例，修复旧 Node 下 `crypto is not defined` 全局 500）。遗留（非功能，待口径统一）：接口文档 00 §2.4 与 ER §2.2 约定知识文档 `doc_`、分块 `chk_` 前缀，后端实现为 `kdoc_`/`kchk_`（ID 对前端不透明、无功能影响） |
| v0.1.5 | 2026-09-10 | #1 通道与基础（前置）：真实环境（API :8080 Node20 + PG/Redis）走查通过置 ✅。① 切真：`.env.development` `VITE_USE_MOCK=false`（MSW 仅契约测试启用），请求经 Vite proxy 打真实后端；② envelope/分页/金额：响应统一 `{code,message,data,traceId}`（错误必带 traceId），列表 `data={items,total,page,pageSize}`（真实复核 `pageSize=5` 回 5 条 / `total=64`），金额经 `formatMoney` 分↔元解析——三者均有契约/单测护栏；③ **错误码管道（本次核心缺口：原为各页面零散 `ElMessage`，列表查询失败被静默吞掉）**：新增 `src/api/error-handler.ts` 单一事实源 `handleApiError(error, opts)`——40001 warning（`onBadRequest` 可读 `details.issues` 内联表单映射）/ 40101 静默（http 层已清会话 + 守卫跳登录）/ 40301 error（`forbiddenMessage` 覆盖，列表渲染空态）/ 40401 warning / 40901 warning（`conflictMessage` 覆盖，乐观回滚由消费方 onError 完成）/ 42201 warning / 42901 warning + Retry-After 秒数 / 50001 `ElNotification` + traceId / 50301 warning 降级提示；`ApiError` 扩展 `retryAfterMs`（解析 `Retry-After` 秒数或 HTTP 日期）与 `details`；`queryClient` 挂 `QueryCache.onError` 兜底全部列表/详情查询（`meta.silentError` 可跳过）；④ 统一改造 20 处调用点（CRM / 客户 360° / 工作台 / 跟进 / 知识 / 审核 / 设置）复用管道，并为设置各页 `load/save/邀请/改角色/停用/邮箱测试/删除` 等原「无 catch 静默失败」路径补齐提示；⑤ 通道鉴权：axios 请求拦截与 `fetch-event-source`（`useTaskStream`）均带 `Authorization: Bearer`。验证：真实后端 register→list 走查 envelope/分页/40001(issues)/40101/40401 均符合接口文档 §2；前端 `vue-tsc` ✅、eslint ✅、vitest 21 文件 175 用例全通过（新增 `tests/unit/error-handler.spec.ts` 16 例覆盖 40001~50301 各 UI 行为与 Retry-After 解析）；**环境记录（重要）**：前端构建/体积门禁必须在 **Node 20** 下执行——`unplugin` 依赖 `import.meta.dirname`，该 API 在 Node 18 为 `undefined`，导致 `vite build` 加载 `vite.config.ts` 即抛 `ERR_INVALID_ARG_TYPE`（与代码无关）；Node 20.20.2 下 `vue-tsc` ✅、`eslint` ✅、`vitest` 21 文件 175 用例 ✅、`vite build` ✅、`size` 首屏 153.6KB/300KB ✅、最大单 chunk tiptap 150.3KB/500KB ✅ |
| v0.1.6 | 2026-09-10 | #7 05 CRM 客户中心：真实环境（API :8080 Node20 + PG/Redis）全链路走查通过置 ✅。① **契约对齐**：后端把 contacts CRUD 与活动列表从 `/customers/*` 迁到契约根级 `/contacts`、`/activities`（05 §2/§3.4）、移除非契约路由，并补齐原先完全缺失的**全局联系人列表** `GET /contacts`（scope 按所属客户 owner 裁剪 + keyword 命中姓名/邮箱/公司名）；活动列表补 `customerId`/`operatorType`/`startDate`/`endDate` 筛选与 `operatorType` 字段。② **删除语义闭环**（原实现与文档相悖：非锁定态直接软删、锁定态才走审批，且 `deleteLocked` 从未被置位→前端「删除待审」链路整体失效）：`DELETE /customers/{id}` 改为不直接删，生成 `customer_delete` 审批（riskLevel=high、context 带 relatedCounts）并置 `deleteLocked` 锁定态；重复删除→40901；`batch-delete`→`{approvals,failed}`；审核中心批准→落 `deletedAt` 真删、拒绝→**自动解锁**。③ **响应形状对齐前端类型**：`batch-owner` 由 `{results}` 改为 `{updated}`（原形状会让前端 `resp.failed.length` / `resp.updated` 直接抛错）。④ **列表/详情字段补齐**：前端列表行被**直接用作编辑表单数据源**，缺字段会在保存时把真实值覆盖为空（如把正式客户翻成潜在），故补 `ownerName`/`isFormal`/`industry`/`website`/`remark`/`deleteLocked`/`createdAt`/`updatedAt`，详情补 `deleteLocked`/`contactsCount`；并补「锁定态不可编辑→40901」（与 mock §3.3 口径一致）。⑤ **联调修复 2 后端缺陷**：**#1 联系人邮箱重复返回 50001**（仅靠 DB 唯一约束 `uq_contact_org_email` 兜底、异常未捕获）→ create/update/新建子表单三处加 org 内邮箱唯一预检，按 mock 口径返回 **40901**「该邮箱已被其他联系人使用」；**#2 客户存在活动时 `GET /customers` 整体 500**（`lastActivityAt` 取自原始 SQL 聚合 `max(created_at)`，pg 驱动返回 string，直接 `.toISOString()` 抛错）→ 统一 `toIso` 归一化（列表 + 详情）。⑥ **环境修复（本次核心，影响全部模块）**：a) 应用缺失迁移（drizzle 0003/0004 + manual 0006~0008）——本机库仅应用 2/4 条、缺 `notification` 等表，是此前多项集成测试 42P01 失败的共同根因；b) **运行时 `DATABASE_URL` 由 superuser 切换为 `tradepilot_app`**——原连接角色 `tradepilot` 具备 `rolsuper + rolbypassrls`，**RLS 被整体绕过**（实测新注册租户 `GET /customers` 返回全库 80 条＝跨租户数据泄漏；换 `tradepilot_app` 后复测：未设 `app.org_id` 为 0 条 fail-closed、置租户后仅 11 条），另补 `MIGRATE_DATABASE_URL` 保留迁移 owner 权限并更新 `.env.example` 注释。⑦ **验证**：真实后端 register→列表字段→阶段机（合法流转 / 非法与同态→40901）→联系人 CRUD（重复邮箱 40901 / keyword / 编辑 / 删除）→活动流 `operatorType`→软删走审批（锁定态重复删除、编辑均 40901）→`approvals/summary`→拒绝自动解锁→批量删除（2 成功 + 1 失败）→批量改派 `updated=2` 且 `ownerName` 联动→批准真删 + 审批日志留痕，全链路符合 05/12 文档；后端 m5-b1 集成测试 **33/33** ✅（新增 6 例回归：列表契约字段 / `lastActivityAt` 聚合、详情 `deleteLocked`+`contactsCount`、邮箱唯一 40901×2、锁定态编辑 40901）、全量 **209/212**、`typecheck` 16/16 ✅、`lint` ✅；前端 `vue-tsc` ✅、`eslint` ✅、vitest 21 文件 175 用例 ✅。遗留（非本次范围）：全量测试余 3 项失败均与本项无关——本机常驻 `apps/worker`（PID 12966）抢消费队列，致 `tasks` claim 乐观锁断言与 11 知识中心索引状态断言失败（与 v0.1.4 记载同因，复跑需隔离 Redis DB）；`batch-owner` 越权 40301 由服务层集成测试覆盖（HTTP 层需第二账号，未做浏览器点击级走查） |
| v0.1.7 | 2026-09-10 | #8 03 AI 获客（`lead-gen`）：真实环境（API :8080 Node20 + Worker + PG/Redis）全链路走查通过置 ✅。① **Worker 启动崩溃修复（本次核心基础项）**：`PostgresSaver.setup()` 首句 `CREATE SCHEMA IF NOT EXISTS langgraph` 需数据库级 CREATE 权限，而运行时角色 `tradepilot_app` 无 DDL 权限，Worker 启动即报 42501 `permission denied for database` 崩溃——checkpoint 4 表 DDL 下沉到 manual 迁移 `0009_manual_langgraph_checkpoint_tables.sql`（以表 owner 建立并授权），运行时 `createCheckpointer` 新增 `CheckpointerOptions.provisionSchema`（默认 `false`，仅依赖升级补列时置 true），env 补 `LANGGRAPH_CHECKPOINT_SETUP`（用 `.enum(['true','false']).transform(...)` 解析，规避 `z.coerce.boolean` 把 `'false'` 解析为 `true` 的陷阱）。② **走查**：自然语言创建→`parse` 结构化回填（confidence/reasons）→创建 `lead_hunting` 任务→SSE 日志流实时滚动→done 刷新；发现列表评分分档（InsightCard hover）；**批量加入 CRM**（域名三级去重、duplicate 分流、mapped 不归属）；已转化行禁用，均符合 03/14 文档。③ **联调修复 7 处契约/链路缺陷**：**#1 `employeeId` 应为选填**（原必填致创建失败）；**#2 `summary` 缺字段**；**#3 `?inCrm=false` 被解析为 true**（`z.coerce.boolean` 陷阱→改三态布尔解析）；**#4 convert 响应形状**（由批量改为单条 `{leadId,customerId,customerName,mapped}`，重复提交 `mapped=true`，对齐 04 §2）；**#5 360° 客户双源聚合**；**#6 `ownerId` 校验**；**#7 同名公司串数据（最关键）**——身份原以 `companyName` 为键，两轮同名 mock 公司互相覆盖致联系人混入、域名恒空（修复前 1 条 lead/10 联系人，修复后 2 条 lead/联系人各 5 条且 0 越组）——统一为**域名优先、名称兜底**归并键（`LeadContact` 增 `domain?`，`search-tools`/`flows` 统一 `entityKey()`，`LeadIdentityMap` 改为按去重键索引，`recordScore`/`assembleLeads` 同步按去重键连接）。④ **验证**：`full-chain` 全链路集成测试 **9/9** ✅（新增「同名不同域名不串数据」回归断言）；`m5-b3` convert 按新契约更新；全量 **223/223** ✅、`typecheck` 16/16 ✅。**环境记录（重要）**：复跑前须清理本机常驻的**双 Worker 实例**（真实进程名为 `tsx/dist/cli.mjs watch src/index.ts`，`pkill -f 'apps/worker'` 不匹配），否则测试任务被另一实例抢占消费致 `scheduled→running`，本实例 Runner claim 落空返回 `skipped`（`expected 'skipped' to be 'completed'`）。遗留（属 07 跟进模块，待排期）：`follow_up` 调 `knowledge_search` 报「员工未被授权使用工具」、`ai_task` 累计 3678 条失败 |
| v0.1.8 | 2026-09-10 | #9 02 AI 员工中心（`ai-employees`）：真实环境（API :8080 Node20 + Worker + PG/Redis）走查通过置 ✅。① 六卡片墙真实渲染：全量 6 卡（status 语义色 / `currentTask` 轻量对象 / KPI 进度 / `workspacePath`，D4 跟单+经理占位入口禁用、KPI 占位文案）符合 02 §1.1；任务详情抽屉复用 TaskWorkspace 日志流（#5 基座）；分步创建向导以真实 `GET /roles` 模板预填（sopParamDefs select 分档派生）。② **补齐前端缺失功能：员工级暂停/恢复**（02 §3.4 P0，此前前端完全未实现）——新增 `pauseEmployee`/`resumeEmployee` API + `PauseEmployeeResp`/`ResumeEmployeeResp` 类型 + MSW handler（pause→idle+留痕语义、resume→清 statusDetail）+ 卡片操作按钮（working→暂停 / 其余→恢复，canManage 裁剪）+ i18n 中英文案；真实接口验证契约 `{employeeId, pausedTasks}` / `{employeeId, resumedTasks}`（浏览器点击 toast「已恢复 0 个任务」，幂等安全；running→paused 置换与 SSE `status=paused` 事件由后端 m5-c 集成测试覆盖）。③ **联调修复 2 后端缺陷**：**#1 `sopParams` 未定义键未校验**（02 §3.2 要求「含模板未定义的参数键返回 42201」，原实现静默合并进 advancedSettings 副本）→ `create` 在解析 SOP 模板后按 `advancedSettings` 键集合校验，未定义键抛 42201，新增集成回归用例；顺带修正既有测试用例「sales 角色误带 `match_product` 参数」的数据合法性问题。**#2 KPI metric 口径漂移**（后端 seed/`ROLE_KPI_METRIC` 用 `daily_insights/reply_rate/followup_completion/risk_alert_timeliness/report_on_time`，违反「前端契约为唯一事实源」约定）→ 统一为接口文档 02 §1.1 注口径：`customer_researcher=daily_profiles / sales=daily_replies / follow_up=daily_followups / merchandiser=active_orders / manager=daily_reports`（`register-seed` + dto 同步），非法 metric 回归用例与断言同步更新。④ **验证**：新注册租户 `GET /roles` 六角色 metric 与文档/mock 逐项一致；浏览器走查 6 卡 + 暂停/恢复按钮 + toast 正常；后端 `m5-c` + `m5-b3` 集成测试 **37/37** ✅；前端 `vue-tsc` ✅。**环境记录（重要）**：packages/* 以 **dist 产物**被解析——`register-seed` 等包内改动须 `pnpm --filter @tradepilot/db build` 重建并**重启 API/Worker 进程**（tsx watch 仅监听本包 src，运行进程的 require 缓存不会因 dist 更新而失效），否则新租户仍落旧种子；apps/api 为 tsx 直跑 src 无此问题 |
| v0.1.9 | 2026-09-10 | #10 04 客户 360°（`customers/:id`）：真实环境（API :8080 Node20 + Worker + PG/Redis）全链路走查通过置 ✅。① **联调修复 4 处后端契约/链路缺陷**：**#1（阻塞）`GET /customers/{id}/insights` 返回数组而非契约形状**（04 §3.2 要求 `{purchaseProbability, nextAction}`，原直出 `customer_insight` 行数组 → 前端 `insight.purchaseProbability` 恒 undefined，Insights 页签永久空态、analyze 完成后也不展示）→ 按 Insight Schema 组装：取 `purchase_probability` 行（uq 每类型单行）映射 `{value, confidence, reasons, citations, generatedAt}`，无行返回双 null（前端引导「重新分析」空态）；**#2（阻塞）`GET /customers/{id}/products` 40401**（前端 ProductsPanel 依赖该接口，接口文档 04 §2 清单漏列、后端未实现）→ 补路由（`withOrg` + 越权 40301 校验，产品目录表未落地随 08 P1 暂返回 `[]`，页面空态降级）；**#3 contacts 缺 `customerId`/`companyName`**（前端 `ContactItem` 必填字段）→ 查询带出客户名回填；**#4 `nextAction.type` 越枚举**（`writeCustomerInsight` 固定写流程内部语义 `follow_up`，不在前端 `contact_decision_maker/generate_outreach/send_quote` 三枚举内 → 推荐按钮点击无响应）→ API 组装时归一化到合法枚举（无 targetId 时以主联系人兜底，无联系人退化 `contact_decision_maker`）。② **浏览器真实走查**（VITE_USE_MOCK=false）：头部聚合（公司/官网/负责人/阶段机/语境化动作）；8 页签懒加载——概览（公司信息 + 产品匹配空态降级）、联系人（含 AI 开发信入口）、产品匹配（空态）、会话（outreach 自动建会话）、活动、AI 洞察全链路：**「开始分析」→ `POST /analyze` 异步任务 → Worker `product_analysis`（mock LLM）→ `customer_insight` 写回 → Insights 页签 InsightCard 完整渲染（采购概率/置信度进度/判断原因/分析时间/推荐下一步按钮）**；console 0 errors。③ **前端渲染缺陷修复**：未分析客户头部「AI 价值分」渲染 `null%`（仅判 `undefined` 未判 `null`）→ 改 nullish 判定显示 `—`。④ **验证**：后端 `m5-b3` 集成测试（含 insights 新契约断言 + 无洞察双 null 新用例 + afterAll 清理顺序修正 `customer_insight.task_id` FK 先于 `ai_task`）**16/16** ✅、`m5-c` 37/37 ✅；前端 `vue-tsc` ✅。已知差异（记录待口径统一，非阻塞）：a) `write_customer_insight` 未写 `confidence` 且 `citations` 恒空（前端置信度显 0% + 低置信角标；证据链引用随 07 知识 RAG 落地）；b) mock LLM 对 `copilotSchema.purchaseProbability`（0~100 百分比语义）产出 0.75 → round 后 1%，前端显「采购概率 1%」，属 mock 数据质量非契约问题；c) Overview `companySize/foundedYear/mainProducts` 无数据源（08 产品中心 P1），前端 `—` 降级符合 04 §1.2 选填口径 |
