# M4 剩余部分交付总结

> 交付时间：2026-09-08（C8 收口与 A2/A3 测试补齐：2026-09-09）· 分支：feautre-mvp · 门禁：lint/typecheck/test 全绿（31/31 任务，144 测试通过）

## 交付范围（C2–C8 + A1）

### C2 · QuotaReset org 时区日界重置 + 搜索令牌桶

- `packages/tools/src/registry.ts` `assertQuota`：员工级外部配额日 key 改为按 **org 时区墙钟日**（`zonedDayKey`）分片，当地零点自动轮换，TTL 48h 过期；时区取 `TaskRunContext.org.timezone` 快照。
- `packages/tools/src/builtin/quotas.ts`（新）：`assertOrgSearchQuota` org 级搜索/抓取日额度令牌桶（`orgsearch:{orgId}:{day@tz}`）+ `getOrgTimezone` 60s memo + `configureOrgSearchQuota`（env `ORG_SEARCH_DAILY_LIMIT` 默认 2000）。
- `apps/worker/src/scheduler/quota-reset.ts` 重写为 `QuotaResetScanner`：每 5min 扫 org，按 timezone 计算 `zonedDayKey`，SET NX 日界标记留痕。
- `packages/core` 新增 `zonedDayKey` 纯函数 + 单测。

### C3 · RAG 入库流水线（q:knowledge_index）

- `packages/integrations/src/embedding/`（新）：`OpenAiEmbeddingProvider` + 进程级注入。
- `packages/integrations/src/storage/`（新）：`S3Storage` 最小 SigV4 客户端（无 aws-sdk，MinIO 兼容）+ `configureObjectStorage`。
- `apps/worker/src/queues/knowledge-index.ts`（新）`KnowledgeIndexProcessor`：取原文→parse（pdf→unpdf / docx→mammoth / md·txt 直读）→clean→chunk→embed（≤100/批）→事务落库（旧 chunk 物理清除 + 批量插入 + status='indexed'），失败→failed+error。
- `enqueue.ts` 增 `enqueueKnowledgeIndex(docId)`；`processor.ts` 双语义分流；worker deps：unpdf、mammoth。

### C4 · knowledge_search 混合检索实装

- `packages/tools/src/builtin/knowledge-search.ts`（新）`searchKnowledgeChunks`：三路召回（pgvector HNSW + tsquery 全文 + pg_trgm similarity）+ RRF 融合（k=60）+ score 归一 + 场景差异化；AI 工具与 11 API 共用。
- `crm-tools.ts` `knowledgeSearchTool` 重写（替换 ILIKE 骨架，返回 score；noResult 强制提示禁止编造）。

### C5 · 11 知识中心 API（7 接口）

- `apps/api/src/knowledge/`（新）：documents 上传（multipart+magic number）/列表/详情（软删回溯）/软删（chunk 物理清除）/retry/stats/search；删除与重试 @Roles manager/admin。

### C6 · 三图真实工具（搜索/抓取供应商适配）

- `packages/integrations/src/search/`（新）：`HttpSearchProvider`（Serper 兼容 + 轻量 HTML 抓取）+ 进程级注入。
- `search-tools.ts` `web_search`/`site_crawl` 接 `getSearchProvider()` + `assertOrgSearchQuota`。email_send 真实外发链路 M3 已完成（确认未回退）。

### C7 · 12 审核中心 API（6 接口 + resume 回调闭环）

- `apps/api/src/approvals/`（新）：summary/list/detail/approve（approve/edited_approved + 字段级 editedDiff 留痕 → `markResumed` + `enqueueResume` 恢复图执行）/reject（必填 reason + 级联 ai_task.failed(approval_rejected)/follow_up paused + SSE）/logs。
- `enqueue.ts` 增 `enqueueResume(taskId, taskType, {nodeId, approvalId})`；`runner.ts` 增失败堆栈日志。

### C8 · 安全基线

- `apps/api/src/common/middleware/rate-limit.middleware.ts` + `security.module.ts`（新）：Redis 固定窗口 60s 按 IP 限流（默认 600/min，env 可调，探针豁免，fail-open）。
- `packages/tools/src/builtin/content-compliance.ts`（新）：email_send 外发前内容合规钩子（内置基线 + 可注入扩展），违规 42201。
- Break-up 强制人工（ApprovalGate 既有）+ 审计串联（approval_request/log + ai_task(_log) + customer_activity）已随链路落地。
- **获客合规收口（2026-09-09，08 §7）**：`integrations/src/search/` 实装 robots.txt 尊重（解析失败视为允许 + 10min 缓存）、单站限速（per-host 最小间隔 2s，`PerHostRateLimiter`）、UA `TradePilotBot` 标识、`excludeDomains` 硬过滤（含子域）；数据最小化维持「只存 title/desc 摘要，不整页入库」。纯函数单测 `integrations/test/search-compliance.spec.ts`。

### A1 · 清理遗留

- 删除 `apps/worker/repro-branch.ts`。

## A2/A3 测试遗留补齐（2026-09-09）

- **A2 · SSE API 层测试（M3-T4 清零）**：`apps/api/test/task-stream.integration.spec.ts`——任务不存在 40401、已终态开流补发 status+done 并关闭（M3-03）、logId 双通道去重（M3-09）、M3-11 兜底轮询（DB 终态但 done 丢失 → 5s 内按 DB 补发并关闭）、单用户连接上限 42901 + 断开计数回收。
- **A3 · 扫描器独立 spec（M3-T2 清零）**：
  - `apps/worker/test/approval-expiry.integration.spec.ts`：expired 终态 + 系统代理留痕 + 级联 failed(approval_expired)/follow_up paused + 员工回 idle 前置校验（M3-06）+ q:notify + SSE、未到期不动、防重不重复留痕/发通知；
  - `apps/worker/test/zombie-reaper.integration.spec.ts`：心跳缺失收割 failed(timeout) + 员工回 idle + 心跳清理 + SSE、心跳存活不收割、新鲜任务不扫描、员工占用保持状态（M3-06）；
  - `apps/worker/test/delayed-reconciler.integration.spec.ts`：到期无 job 补投、活跃 job 跳过、员工 running/waiting_approval 占位不补投（M3-06 同口径）、org ≥10 满载不补投、未到期不扫描、补投幂等。

## M4 出口验收补齐（2026-09-09）

- **三图 dry-run（D-1，GreenMail 测试邮箱）**：`apps/worker/test/m4-dryrun.integration.spec.ts`——
  ① lead_hunting：真实搜索供应商 → web_search→评分→联系人→crm_write 落 ai_lead，LLM 记账全节点；
  ② email_reply：来信真实 SMTP 投递 → EmailSyncProcessor 真实 IMAP 同步入库 → 派发 → 图执行挂起审批 → 批准 resume → 真实 SMTP 外发 → 收件方 IMAP 收到（全真实链路闭环）；
  ③ follow_up：autoApprove 直发（auto_approved 留痕）→ 真实外发到达 → schedule_next 频控顺延排期第 2 步。
- **知识入库流水线（D-2）**：`apps/worker/test/m4-knowledge-index.integration.spec.ts`——md parse→chunk→embed（确定性 1536 维）→落库 indexed；retry 重跑幂等（旧 chunk 物理清除后重插数量一致）；软删迟到 job→skipped(deleted)；文档不存在→skipped(missing)；存储取文失败→failed+error 留痕。
- **11/12 接口集成（D-2/D-3）**：`apps/api/test/m4-knowledge-approvals.integration.spec.ts`（12 用例）——知识上传（白名单+magic number）/列表/stats/混合检索 citations（docId/docName/category/score）/详情/软删（chunk 物理清除+检索实时失效）/重复删除 40901/retry 仅 failed；审核 summary（Tab count>0）/list/detail 40401/approve（任务恢复 running+log 留痕）/edited_approved（字段级 editedDiff）/reject（级联 failed(approval_rejected)+员工回 idle+follow_up paused）/expired 处置 42201/重复处置 40901。
- **真实生产 bug 修复（测试暴露）**：BullMQ 5 自定义 jobId 禁含 `:`——`enqueueKnowledgeIndex` 的 `kidx:{docId}` 与 `enqueueEmailSync` 的 `mbxsync:{mailboxId}` 均改为 `.` 分隔（上传知识必然触发入队失败，此前无集成覆盖未暴露）。
- **测试基建**：docker-compose `mailpit` 服务替换为 GreenMail 2.1.3（Mailpit 无 IMAP；SMTP 1025 / IMAP 1114 / Web UI 8025）。
- fixture 教训：知识文档测试文本不可用同一文本 `.repeat(3)` 构造——clean 会把「出现 ≥3 次的短行」当页眉页脚剔除致空文本；需按真实文档构造分节内容。

## 基础设施

- 环境变量契约（`shared/contracts/env.ts`）：api+worker 补 S3/EMBEDDING/SEARCH/ORG_SEARCH_DAILY_LIMIT（缺省对齐 docker-compose MinIO 本地值）。
- 启动注入：`apps/api/src/main.ts`（嵌入+存储）、`apps/worker/src/index.ts`（嵌入+存储+搜索+org 配额）。
- `core` 新增纯函数 + 单测：`text-chunk.ts`、`rrf.ts`、`zonedDayKey`。

## 测试卫生修复（既有缺陷，非 M4 引入）

- `apps/{api,worker}/vitest.config.ts`（新）：`fileParallelism:false` + 全局 crypto 兜底 setup（`@langchain/core` rng 依赖 `globalThis.crypto`，Node 18 隔离子进程不暴露，原 M3 集成测试在本机因此全红）。
- `full-chain.integration.spec.ts` 第二个 afterAll 补全 follow_up/approval/message/conversation 清理（原漏删跨文件污染 scheduler 扫描）。
- `scheduler.integration.spec.ts` beforeAll 加 `TRUNCATE ... CASCADE` 全局基线（扫描器跨租户，需干净基线）。

## 门禁

`pnpm turbo run lint typecheck test`：31/31 任务成功，**144 测试通过**，0 错误（`@tradepilot/db:lint` 6 个既有 `no-console` warning，非本次引入）。
