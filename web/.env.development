# 接口基路径：走同域反代，避免 CORS（01 §5.1）
VITE_API_BASE=/api/v1
# 后端开发网关（vite proxy 目标）
VITE_PROXY_TARGET=http://localhost:8080
# 编译期特性档位：p0 | p1（AD-4）
# 本地 dev 默认 p1：P1 模块（08/09/10 等）已进入本地联调，无需再手动覆盖 VITE_FEATURES_PROFILE
VITE_FEATURES_PROFILE=p1
