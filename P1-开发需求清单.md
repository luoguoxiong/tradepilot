# TradePilot AI · P1 开发需求清单（Checklist）

| 项 | 内容 |
|---|---|
| 来源 | [00-产品总览与MVP规划 §5 P1](./产品需求文档/00-产品总览与MVP规划.md)、各模块需求文档、[页面级字段与接口文档](./产品需求文档/页面级字段与接口文档/00-总览与接口规范.md)、[PostgreSQL ER 图](./产品需求文档/PostgreSQL%20数据库%20ER%20图/00-数据库设计规范与总览.md)、[后端技术方案文档](./后端技术方案文档/00-总纲与技术选型.md)、[LangGraph 工作流 §8](./产品需求文档/LangGraph%20工作流/00-P0工作流定义.md) |
| 范围 | `08 产品中心 / 09 报价中心 / 10 订单中心 / 13 AI外贸经理 / 14 AI任务中心（完整版）/ 15 数据中心`，及 `16` P1 配套、`02` 卡片启用、`00 §5.1` 降级矩阵恢复（D1～D14） |
| 分支 | `feautre-mvp`（P1 实施线，M5+） |
| 前置基线 | P0（M1–M4）已交付：[server/M4-交付总结.md](./server/M4-交付总结.md)；DB 40 表 DDL 与 Drizzle schema 已落地（P1 无需新建表，见 §5） |
| 交付模式 | 前端 `VITE_FEATURES_PROFILE=p1` 构建期放量；不做运行时开关 UI（[features.ts](./web/src/features.ts) / 前端技术方案 02 §5.1） |
| 门禁 | `pnpm turbo run lint typecheck test` 全绿 + `VITE_FEATURES_PROFILE=p1` 构建通过 |

---

## 0. 使用说明

- 编号规则：`P1-<模块号>-<序号>`，跨模块任务用 `P1-X-<序号>`。
- 每条任务完成后勾选 `[x]`；模块 DoD = 该模块全部勾选 + §6 门禁通过。
- 权威分工：字段/接口以「页面级字段与接口文档」为准，表结构以「ER 图」为准，需求行为以「产品需求文档」为准；实现细节以「后端/前端技术方案」为准，冲突时以产品侧为准并回写文档。
- 状态标记：`[ ]` 待办 / `[x]` 完成；`⛔` 表示被 §7 待拍板项阻塞。

---

## 1. 交付范围与现有基线

| 模块 | feature key | 前端路由 | 需求文档 | 接口文档 | ER |
|---|---|---|---|---|---|
| 08 产品中心 | `products` | `/products`、`/products/:id` | [08](./产品需求文档/08-产品中心.md) | [08](./产品需求文档/页面级字段与接口文档/08-产品中心.md) | [06](./产品需求文档/PostgreSQL%20数据库%20ER%20图/06-产品与知识模块.md) |
| 09 报价中心 | `quotes` | `/quotes`、`/quotes/:id` | [09](./产品需求文档/09-报价中心.md) | [09](./产品需求文档/页面级字段与接口文档/09-报价中心.md) | [07](./产品需求文档/PostgreSQL%20数据库%20ER%20图/07-报价与订单模块.md) |
| 10 订单中心 | `orders` | `/orders`、`/orders/:id` | [10](./产品需求文档/10-订单中心.md) | [10](./产品需求文档/页面级字段与接口文档/10-订单中心.md) | [07](./产品需求文档/PostgreSQL%20数据库%20ER%20图/07-报价与订单模块.md) |
| 13 AI外贸经理 | `manager` | `/manager` | [13](./产品需求文档/13-AI外贸经理.md) | [13](./产品需求文档/页面级字段与接口文档/13-AI外贸经理.md) | [08](./产品需求文档/PostgreSQL%20数据库%20ER%20图/08-审核与数据中心模块.md) |
| 14 AI任务中心（完整版） | `taskCenter` | `/tasks` | [14](./产品需求文档/14-AI任务中心.md) | [14](./产品需求文档/页面级字段与接口文档/14-AI任务中心.md) | [02](./产品需求文档/PostgreSQL%20数据库%20ER%20图/02-AI员工与任务模块.md) |
| 15 数据中心 | `dataCenter` | `/data-center` | [15](./产品需求文档/15-数据中心.md) | [15](./产品需求文档/页面级字段与接口文档/15-数据中心.md) | [08](./产品需求文档/PostgreSQL%20数据库%20ER%20图/08-审核与数据中心模块.md) |
| 16 配套（P1） | — | `/settings/*` | [16](./产品需求文档/16-系统设置.md) | [16](./产品需求文档/页面级字段与接口文档/16-系统设置.md) | [01](./产品需求文档/PostgreSQL%20数据库%20ER%20图/01-企业与用户设置模块.md) |

**现有基线（P0 已就位）**：

- 后端 `apps/api/src` 已有：`auth/customers/conversations/dashboard/employees/follow-ups/knowledge/leads/org/settings/tasks/approvals`；**缺** `products/quotes/orders/manager/analytics`。
- 后端 `apps/worker` 已有队列：`lead_hunting/email_reply/follow_up/knowledge_index/email_sync/notify`；**缺** `q.analysis`（`business_analysis/order_monitor`）与 `product_analysis`（见 [04 §2](./后端技术方案文档/04-任务调度与队列.md)）。
- 前端路由与 `features.ts` 占位已注册（`/tasks /products /quotes /orders /manager /data-center` + 设置项 `crm-integration/pricing-rules/api-keys`），**缺** 页面实现与 `api/resources`。
- ER schema 文件（`packages/db/src/schema/06-product-knowledge.ts`、`07-quote-order.ts`、`08-approval-data.ts`）已定义对应表，迁移 0000 已含建表语句。

---

## 2. 交付顺序与依赖（建议）

> 修复评估中发现的「P1 内部依赖未规划」问题：按下列阶段推进，前一阶段为后一阶段前置。

```text
S1 横切底座   定价引擎 + 事件/通知 + Webhook 出站 + 任务中心完整操作 + 分析预聚合
      │
S2 产品中心   08（产品/价格档/资料/AI 知识）  ← 09 定价输入前置
      │
S3 报价订单   09（报价+定价建议+PDF） → 10（订单+风险）  ← 10 依赖 09 转单
      │
S4 分析层     14 完整版 → 15 数据中心 → 13 外贸经理       ← 13 依赖 15 指标底座
      │
S5 恢复收口   16 FR-06/FR-11 + 02 卡片启用 + 00 降级矩阵 D1～D14 恢复（16 FR-07 已于 S1 前置交付）
```

- [x] **P1-X-01** 阶段划分与排期评审通过（含 §7 阻断项拍板 → 见 §7 决策表）。
- [x] **P1-X-02** `16 FR-07 产品与报价规则` 纳入 S1（09 定价引擎强依赖，不能延后）。

---

## 3. 横切能力（S1 先行）

### 3.1 定价引擎 `pricing_engine`

> 权威口径：[09 §7.1](./产品需求文档/09-报价中心.md)、[09 接口 §3.1/§3.2](./产品需求文档/页面级字段与接口文档/09-报价中心.md)、[ER 07 §4](./产品需求文档/PostgreSQL%20数据库%20ER%20图/07-报价与订单模块.md)

- [x] **P1-X-10** 实现五项成本核算：`purchase = product.cost_price`、`freight/insurance/tax` 按 16 报价规则、`fx` 按报价时点汇率。
- [x] **P1-X-11** 利润率核算与利润红线校验（低于 `profitFloorPct` → `42201`，100% 拦截）。
- [x] **P1-X-12** 汇率快照三元组 `{rate, date, source}`（MVP `source='manual'`，16 维护）。
- [x] **P1-X-13** 行级 `cost_snapshot` 固化（`quotation_item`）+ 报价级 `costBreakdown` 汇总。
- [x] **P1-X-14** 输出 `reasons[]`（数量/客户评分/历史成交），对齐 Insight Schema。
- [x] **P1-X-15** 定价引擎单测：五项成本、红线拦截、快照不随市价变动。

### 3.2 事件 / 通知服务 + Webhook 出站

> 依据：[04 §2 `q.notify`](./后端技术方案文档/04-任务调度与队列.md)、[16 FR-11](./产品需求文档/16-系统设置.md)

- [x] **P1-X-20** `q.notify` 发送端实装（站内 + 邮件），P0 仅留痕部分补齐。<!-- M5-A2 已实装 `apps/worker/src/queues/notify.ts`（site 落库 + email 走 org connected mailbox，无邮箱降级仅站内）；本轮补 Webhook 派生 -->
- [x] **P1-X-21** Webhook 出站：事件订阅（`webhooks[{url, events[], secret}]` + `secret_enc` 密文）。<!-- 事件注册表 `shared/contracts/open-api.ts`；订阅 CRUD `apps/api/src/open-api/webhook.service.ts`（AES-256-GCM 入库、永不回显）；投递链路 notify → q:webhook（attempts=5 指数退避 1m 起、10s 超时、`X-TP-Signature`）、适配器 `packages/integrations/src/webhook-out/`、消费者 `apps/worker/src/queues/webhook.ts`、manual `0013`（`apikey_lookup` RLS） -->
- [x] **P1-X-22** 开放 API 中间件（`apiKey` + `scopes` 鉴权），配合 16 FR-11。<!-- `x-api-key` 分支并入 JwtAuthGuard + `ScopesGuard`（fail-closed）；`ApiKeyService` 哈希校验/停用即时失效/60 req/min 限流(42901)/`last_used_at` 记账；CRUD `GET|POST|DELETE /settings/api-keys`（明文仅创建返回一次）；业务端点的 scopes 标注随 S5 路由接入 -->

### 3.3 任务中心完整操作（14 P1 部分）

> 依据：[14 §7.3](./产品需求文档/14-AI任务中心.md)、[04 §3.2/§5.4](./后端技术方案文档/04-任务调度与队列.md)

- [x] **P1-X-30** `pause / resume / cancel`：暂停图 + 恢复执行，状态机联动。<!-- `POST /tasks/{id}/pause|resume|cancel`（`TasksService.{pause,resume,cancel}`）：pause running/scheduled→paused（释放员工位 + `removeTask` 清 delayed job + SSE status=paused，执行中任务下个节点探测中止、检查点保留）；cancel running/scheduled/paused→canceled（补 finished_at + 释放员工位 + SSE status+done，waiting_approval 交审核中心）；resume 分流——已产生检查点（started_at 非空）paused→running + `enqueueResumeFromPause`（`job.data.fromPause`，Runner `claim` paused→running 乐观锁 + `graph.invoke(null)` 从检查点续跑当前节点，工具 taskId+nodeId 幂等），从未执行则 paused→scheduled 交 Dispatcher。证据：`apps/api/src/tasks/tasks.{service,controller}.ts`、`packages/runtime/src/runner.ts`、`packages/runtime/src/enqueue.ts`、`apps/worker/src/queues/processor.ts` -->
- [x] **P1-X-31** `transfer-to-human`：暂停图 + 写 outputs 交接摘要。<!-- `POST /tasks/{id}/transfer-to-human`：running→paused（中断图）+ 释放员工位 + `outputs` 追加 `{type:'handoff',payload:{fromStatus,reason,summary,assignee,transferredAt,transferredBy}}`（append 语义、可回溯、缺省摘要兜底）；paused/failed 仅追加摘要不改状态。证据：`apps/api/src/tasks/tasks.service.ts` -->
- [x] **P1-X-32** 恢复 04 中预留的 `enqueue.removeTask` 与手动暂停 API。<!-- `TaskEnqueuer.removeTask` 用于 pause/cancel 清理终态 delayed job；`enqueueResume`/`enqueueResumeFromPause` 先移除终态历史 job 再按 jobId=taskId 重投（避免 BullMQ 幂等 no-op 吞掉恢复）；手动暂停/恢复/取消 API 已接线 `tasks.controller.ts` -->
- [x] **P1-X-33** 失败批量处理（多选重试/转人工）。<!-- `POST /tasks/batch`（`batchTaskActionSchema`：action ∈ retry|transfer_to_human、taskIds 1..50）：逐条复用单任务语义，单条失败不阻断、逐条回传 {ok,status,newTaskId,error} + succeeded/failed 汇总。证据：`apps/api/src/tasks/tasks.{service,dto,controller}.ts`；集成测试 `apps/api/test/tasks-ops.integration.spec.ts`（8 例） -->

### 3.4 分析预聚合 `analytics_daily_summary`

> 依据：[ER 08 §2.5](./产品需求文档/PostgreSQL%20数据库%20ER%20图/08-审核与数据中心模块.md)、[15 §7.1](./产品需求文档/15-数据中心.md)

- [x] **P1-X-40** 定时 ETL 写 `analytics_daily_summary`（`UNIQUE(org_id, stat_date, country, employee_id)`）。✅ `AnalyticsEtl`（每小时/按 org 时区回算 2 个当地日/唯一键 `ON CONFLICT DO UPDATE` 幂等），`server/apps/worker/src/scheduler/analytics-etl.ts`、`apps/worker/src/index.ts`。
- [x] **P1-X-41** 指标口径：基础指标按业务表区间计数；`foundCustomers/repliedEmails/promotedInquiries/savedHours` 按 §7.1 定义。✅ 口径单点实现于 `server/packages/core/src/analytics.ts` + ETL 区间 SQL，物化 org 级 `(ALL,ALL)` 与员工级 `(ALL,emp)`。
- [x] **P1-X-42** `savedHours` 估算口径常量（获客 10min/客户、邮件 15min/封、跟进 5min/次）+ `caliberNote` 随接口返回。✅ `SAVED_HOURS_BASELINE_MINUTES` / `computeSavedHours` / `ANALYTICS_CALIBER_NOTE`（REST 随 15 P1-15-xx 接入）。
- [x] **P1-X-43** `promotedInquiries` 归因：AI 外联后 14 天窗口、最后触点归因。✅ `PROMOTED_INQUIRY_WINDOW_DAYS=14` + `isWithinAttributionWindow` / `pickLastTouch` 纯函数 + ETL CTE（会话首条来信 × 窗口内最近外联 → `customer_activity.operator_id`）。

---

## 4. 分模块清单

### 4.1 模块 08 · 产品中心（`products`）

**后端 API**（接口文档 08 §2）

- [x] **P1-08-01** `GET /products`（keyword/category/status，分页）。
- [x] **P1-08-02** `POST /products`（SKU 同 org 唯一 → `42201`；保存触发知识中心归档）。
- [x] **P1-08-03** `GET /products/{id}`（含 Specifications/Pricing/Documents/AI Knowledge 全页签）。
- [x] **P1-08-04** `PUT /products/{id}`。
- [x] **P1-08-05** `POST /products/{id}/documents`（multipart，`doc_type` 四类）+ `DELETE .../documents/{fileId}`。
- [x] **P1-08-06** `POST /products/{id}/analyze`（异步，返回 `taskId`，不落库）。
- [x] **P1-08-07** `POST /products/{id}/knowledge/generate`（→ `draft`）+ `.../knowledge/confirm`（→ `approved`）。
- [x] **P1-08-08** 成本隔离校验：`cost_price` 不进生成 prompt；`analyze`/`generate` 分工（预览 vs 落库）。

**Worker / Workflow**

- [x] **P1-08-09** `product_analysis` 图：产品资料解析 → 结构化入库 → 知识索引（内部无高风险节点）。
  > 实施偏差（已确认）：`task_type=product_analysis` 已被 M5-C4 客户/线索分析占用，故 08 新增 `task_type=product_knowledge` SOP（`flows.ts`：`load_product_context → generate_knowledge → write_product_knowledge`），语义等价且不动既有客户分析。
- [x] **P1-08-10** AI Knowledge 生成任务落 `product_knowledge`（`status=draft` + `citations`）。
- [x] **P1-08-11** 产品资料归档 `knowledge_document(source='product')` 并回写 `indexed/knowledge_doc_id`。

**前端**

- [x] **P1-08-12** 产品列表页（搜索/分类筛选/状态、添加产品）。
- [x] **P1-08-13** 产品详情页（5 页签：Overview/Specifications/Pricing/Documents/AI Knowledge）。
- [x] **P1-08-14** 添加/编辑产品表单 + AI 分析/生成确认交互。
- [x] **P1-08-15** `api/resources/products.ts` + 类型 + i18n + 路由替换占位。

**边界/验收**：AI 不改基础字段与价格；`citations` 可回溯；变体=独立 SKU（`parent_id` 聚合视图不做）。

### 4.2 模块 09 · 报价中心（`quotes`）

**后端 API**（接口文档 09 §2）

- [ ] **P1-09-01** `GET /quotes`（status Tab）+ `POST /quotes`（`quantity<MOQ`、`validUntil<=今天` → `42201`）。
- [ ] **P1-09-02** `GET/PUT /quotes/{id}`（仅 `draft` 可编辑，否则 `40901`）。
- [ ] **P1-09-03** `POST /quotes/{id}/ai-pricing`（走 §3.1 定价引擎）。
- [ ] **P1-09-04** `POST /quotes/{id}/submit`（→ `waiting_approval` + 生成 `quote` high 审批）。
- [ ] **P1-09-05** `POST /quotes/{id}/send`（前置审批 `approved/edited_approved`，否则 `40901`；写客户活动）。
- [ ] **P1-09-06** `POST /quotes/{id}/mark-won`（→ `won`；潜客自动升级正式客户，联动 05 §7）。
- [ ] **P1-09-07** `GET /quotes/{id}/pdf`（服务端渲染报价头 + 16 公司抬头；写 `activity_type=quote`）。
- [ ] **P1-09-08** ⛔ `lost` 状态处理（→ 见 §7-A1）。
- [ ] **P1-09-09** ⛔ 多轮议价 `negotiation-ladder`（让价梯度，→ 见 §7-A2）。

**前端**

- [ ] **P1-09-10** 报价列表（5 Tab + 数量）。
- [ ] **P1-09-11** 报价详情（明细 + AI 定价建议推荐原因必展示 + 提交审核 + 导出 PDF）。
- [ ] **P1-09-12** 新建/编辑报价表单（客户/产品/数量/单价 + 报价头结构化字段）。
- [ ] **P1-09-13** `api/resources/quotes.ts` + 类型 + i18n + 路由替换占位。

**边界/验收**：价格数字非 LLM 生成；`validUntil/incoterms/currency/exchangeRate/paymentTerms` 必填结构化；状态机 `draft→waiting_approval→sent→won`，编辑仅 draft。

### 4.3 模块 10 · 订单中心（`orders`）

**后端 API**（接口文档 10 §2）

- [ ] **P1-10-01** `GET /orders`（status Tab）+ `POST /orders`（手工/`fromQuoteId` 转单，报价须 `won`）。
- [ ] **P1-10-02** `GET /orders/{id}`。
- [ ] **P1-10-03** `PUT /orders/{id}`（变更 → `order_change` 审批；AI 身份 `40301`；审批通过后落列 + 写 `order_progress_log`）。
- [ ] **P1-10-04** `PUT /orders/{id}/progress`（手工填报唯一入口，写 `order_progress_log`）。
- [ ] **P1-10-05** `GET /orders/{id}/risk`（`plannedPct` 时间线性推导 + 阈值判定；缺证据 `risk=normal`）。
- [ ] **P1-10-06** `POST /orders/{id}/risk/execute`（`internal→taskIds` / `customer→approvalId`）。
- [ ] **P1-10-07** `POST /orders/{id}/risk/draft-email`（联动 06 生成延期沟通草稿）。
- [ ] **P1-10-08** 转单 `cost_snapshot` 从报价行原样复制；手工建单引擎核算（缺省 `{}` 不阻塞）。
- [ ] **P1-10-09** ⛔ 定义 `progress → orderStatus` 映射规则（→ 见 §7-A3）。
- [ ] **P1-10-10** 修正 `planSource` 取值不一致（§7-A4）。

**Worker / Workflow**

- [ ] **P1-10-11** `order_monitor` 图：订单状态轮询 → 风险规则 → 异常告警 outputs。
- [ ] **P1-10-12** 进度更新后自动评估风险并产 `order_risk_insight`。

**前端**

- [ ] **P1-10-13** 订单列表（状态 Tab + 风险徽标）。
- [ ] **P1-10-14** 创建订单表单（从成交报价转化 / 手工创建）。
- [ ] **P1-10-15** 订单详情（进度条 + AI 风险分析 + AI 执行建议 / 人工处理）。
- [ ] **P1-10-16** `api/resources/orders.ts` + 类型 + i18n + 路由替换占位。

**边界/验收**：延期预警必须有 `evidence.plannedPct/actualPct`；`delayDays` 标注估算；订单变更走审批。

### 4.4 模块 13 · AI外贸经理（`manager`）

**后端 API**（接口文档 13 §2）

- [ ] **P1-13-01** `GET /manager/overview`（与 15 同源口径）。
- [ ] **P1-13-02** `GET /manager/discoveries`（机会 + 风险 + `evidence`）。
- [ ] **P1-13-03** `POST /manager/discoveries/{id}/execute`（白名单 `start_lead_task` / `enable_reactivation_strategy`）。
- [ ] **P1-13-04** `GET /manager/team-efficiency` + 定义 `kpiPct` 口径（→ §7-A5）。
- [ ] **P1-13-05** `POST /manager/reports/generate`（异步）+ `GET /manager/reports` + `GET /manager/reports/{id}`。

**Worker / Workflow**

- [ ] **P1-13-06** `business_analysis` 图：多源聚合 → 经理分析报告 → `report` outputs（长任务，检查点密集）。
- [ ] **P1-13-07** 报告落 `business_report`（五段结构 + `estimated` 标注 + citations）。

**前端**

- [ ] **P1-13-08** 今日经营概览（四项指标）。
- [ ] **P1-13-09** AI 发现（机会/风险 + 一键执行按钮）。
- [ ] **P1-13-10** AI 团队效率进度条。
- [ ] **P1-13-11** 经营报告生成/列表/详情（五段 Markdown 渲染）。
- [ ] **P1-13-12** `api/resources/manager.ts` + 类型 + i18n + 路由替换占位。
- [ ] **P1-13-13** 页面权限：经理及以上专属。

**边界/验收**：洞察 `evidence` 必填可下钻；一键仅「发起类」动作（可撤销，无对外/不可逆效果）。

### 4.5 模块 14 · AI任务中心（完整版，`taskCenter`）

> P0 已交付：任务列表/详情/日志/流式/retry（见接口文档 14 §开头）。本清单仅 P1 增量。

- [ ] **P1-14-01** 状态 Tab 筛选（all/running/waiting_approval/completed/failed）。
- [ ] **P1-14-02** `+ 新任务` 统一入口（跨员工下发，`type` 决定 SOP 与权限）。
- [ ] **P1-14-03** `pause / resume / cancel / transfer-to-human` 接口与页面操作（复用 §3.3）。
- [ ] **P1-14-04** 失败批量处理。
- [ ] **P1-14-05** 任务详情：步骤/日志/产出物完整展示 + 审批联动跳转。
- [ ] **P1-14-06** `api/resources/tasks.ts` 扩展 + 页面从占位替换为正式视图。

### 4.6 模块 15 · 数据中心（`dataCenter`）

**后端 API**（接口文档 15 §2）

- [ ] **P1-15-01** `GET /analytics/customer-trend`。
- [ ] **P1-15-02** `GET /analytics/market-distribution`。
- [ ] **P1-15-03** `GET /analytics/ai-contribution`（含 `caliberNote`）。
- [ ] **P1-15-04** `GET /analytics/drilldown`（metric 枚举，下钻明细）。
- [ ] **P1-15-05** `GET /analytics/export`（筛选下趋势/分布/贡献导出 Excel；PDF 不做）。
- [ ] **P1-15-06** 数据权限：`scope=self|team|all` 裁剪（00 §4.4）。

**前端**

- [ ] **P1-15-07** 全局筛选（时间/市场/AI 员工）。
- [ ] **P1-15-08** 客户增长趋势图 + 市场分布 + AI 贡献卡（节省工时需标注口径）。
- [ ] **P1-15-09** 图表下钻明细列表 + Excel 导出。
- [ ] **P1-15-10** `api/resources/analytics.ts` + 类型 + i18n + 路由替换占位。

**边界/验收**：所有数字可 `drilldown` 验证；`savedHours` 标注估算。

### 4.7 模块 16 · P1 配套设置

- [x] **P1-16-01** FR-07 产品与报价规则：`productCategories / costItems / profitFloorPct / discountLadder / defaultIncoterms / defaultCurrency / exchangeRateSource`（ER 01 §2.6 单例；`profitFloorPct` 保存即生效）。
- [x] **P1-16-02** FR-06 CRM 集成（小满/富通天下）：授权连接 → 字段映射 → 同步（`syncDirection`）。<!-- 交付「授权连接 + 字段映射 + 同步方向」配置层：GET/POST/PUT/DELETE /settings/integrations（同供应商每 org 唯一 → 40901）；`mapping` 白名单化（05 CRM 口径 9 字段）并写入 `crm_integration.mapping`。依据后端技术方案 06 §6，MVP 不做任何外呼，实际拉取/推送由后续 `CrmDriver { pullCustomers / pushCustomer / mapFields }` 按 `syncDirection` 执行（故 `lastSyncAt` 暂为 null） -->
- [x] **P1-16-03** FR-11 API/Webhook：`apiKey`（明文仅创建返回一次）+ `webhooks[{url, events[], secret}]`。<!-- 后端 `apps/api/src/open-api`（GET/POST/DELETE /settings/api-keys · /webhooks，secret 加密不回显）随 P1-X-21/22 交付；前端 `ApiKeysView.vue` 本轮交付（明文一次性展示 + 复制、scope/事件白名单勾选、撤销/删除） -->
- [x] **P1-16-04** 三处设置页从 `SettingsPlaceholderView` 替换为正式页面。<!-- pricing-rules（`PricingRulesView.vue`）、api-keys（`ApiKeysView.vue`）、crm-integration（`CrmIntegrationView.vue`）全部替换；占位页与 `p1`「即将上线」标记已删除 -->
- [x] **P1-16-05** 后端 `apps/api/src/settings` 扩展：pricing-rules / crm-integration / api-keys 端点。<!-- pricing-rules 已交付（GET/PUT /settings/pricing-rules）；api-keys / webhooks 已交付（`apps/api/src/open-api`，同挂 /settings 前缀）；crm-integration 已交付（GET/POST/PUT/DELETE /settings/integrations） -->

### 4.8 模块 02 卡片启用 + 00 降级矩阵恢复

- [ ] **P1-02-01** AI 跟单员工卡片：解除「即将上线」占位，入口启用（随 10）。
- [ ] **P1-02-02** AI 外贸经理卡片：解除占位，入口启用、KPI 展示（随 13）。
- [ ] **P1-02-03** 02 FR-04 完整任务列表/日志/恢复随 14 启用（从 `currentTask` 轻量摘要升级）。
- [ ] **P1-02-04** 02 KPI 精化口径落地（按业务表精确聚合，替换 P0 的 `ai_task` 计数）。

**00 §5.1 降级矩阵逐项恢复**（按模块交付顺序勾选）

- [ ] **D1** Dashboard KPI 卡：新报价 / 预计成交额（随 09/10）。
- [ ] **D2** Dashboard 今日待处理：报价待审核 / 订单延期风险（随 09/10）。
- [ ] **D3** Dashboard FR-01「AI 每日报告」入口（随 13）。
- [ ] **D4** 02 外贸经理卡片入口（随 13）。
- [ ] **D5** 02 FR-04 完整任务列表（随 14）。
- [ ] **D6** 04 客户360° Quotes / Orders 页签（随 09/10）。
- [ ] **D7** 04 Products 页签行点击跳 08 产品详情（随 08）。
- [ ] **D8** 06 流程型建议「创建报价」（随 09）。
- [ ] **D9** 06 FR-10 业务参数结构化来源（随 08/09）。
- [ ] **D10** 12 审核中心分类 Tab：报价 / 订单变更 / 批量营销（按启用模块渲染）。
- [ ] **D11** 11 产品资料自动归档「产品」分类（随 08）。
- [ ] **D12** 16 FR-07/FR-06/FR-11（见 §4.7）。
- [ ] **D13** 销售中心「开发信 / 邮件模板」导航占位（随独立模块立项，非本期）。
- [ ] **D14** Dashboard「进入待办中心」入口（随 14/15）。

---

## 5. 数据层（ER 校核）

> DDL 已在 P0 阶段一次性落地，本阶段为**校核与补齐**，不新建表。

- [x] **P1-DB-01** 校核 P1 表已在迁移中：`product / product_spec / product_price_tier / product_document / product_knowledge`、`quotation / quotation_item / sales_order / sales_order_item / order_risk_insight / order_progress_log`、`business_report / ai_discovery / analytics_daily_summary`。
- [x] **P1-DB-02** 补齐 manual 迁移：RLS 角色/策略、列注释、跨模块 FK（`quotation.approval_id → approval_request` 等，见 ER 07 §3 / ER 08 §3）。<!-- 0001/0002 已覆盖 RLS/注释/跨模块 FK；本次补 `0012_manual_p1_check_constraints.sql`（12 项 ER 区间类 CHECK） -->
- [x] **P1-DB-03** 枚举校核：`quote_status`（含 `lost`）、`order_status`、`order_risk`、`product_status`、`report_period`、`report_status`、`discovery_type`、`product_doc_type`。
- [x] **P1-DB-04** Drizzle schema 与 ER 逐字段比对（含 `cost_snapshot jsonb DEFAULT '{}'`、`deleted_at/deleted_by`）。

---

## 6. 测试与门禁

- [ ] **P1-T-01** 定价引擎单测（成本/红线/快照/汇率）。
- [ ] **P1-T-02** 09 接口集成：新建→AI 定价→提交审批→approve→send→mark-won→转单 全链路。
- [ ] **P1-T-03** 10 接口集成：手工建单/转单、进度更新→风险判定、变更审批、execute 分流（task/approval）。
- [ ] **P1-T-04** 08 接口集成：SKU 唯一、资料上传/删除、knowledge generate→confirm、cost_price 隔离。
- [ ] **P1-T-05** 13/15 接口集成：指标口径一致性（13 overview 与 15 同源）、drilldown 可验证、Excel 导出。
- [ ] **P1-T-06** 14 P1 操作集成：pause/resume/cancel/transfer + 排队语义（员工并发=1、org 上限 10）。
- [ ] **P1-T-07** 降级矩阵回归：`VITE_FEATURES_PROFILE=p0` 构建下 P1 入口/页签不渲染；`=p1` 全部启用。
- [ ] **P1-T-08** 门禁：`pnpm turbo run lint typecheck test` 全绿。
- [ ] **P1-T-09** P1 dry-run（真实链路）：产品→报价（真实定价）→审批→外发→成交→转订单→进度→风险预警→经理报告→数据中心指标。

---

## 7. 已拍板决策（UX 视角，v1 定稿）

> 决策准绳：**用户能否看懂（可解释）、能否放心（可信/可控）、能否少操作（单一录入点）**。A1～A9 全部拍板，对应文档回写已完成。

| # | 议题 | 拍板结论 | 用户体验依据 | 回写 |
|---|---|---|---|---|
| A1 | 09 `lost`（已失效） | **本期交付最小闭环**：新增 `POST /quotes/{id}/mark-lost`（原因**选填**）+ `POST /quotes/{id}/revive`（复活回草稿）；列表增「已失效」Tab；状态机补 `draft/waiting_approval/sent → lost → draft` | 客户沉默 / 选竞品是高频结局，无失效出口 →「已发送」Tab 无限堆积、漏斗转化率分母虚高、用户无法判断"这单还活着吗"。原因必填会增加摩擦，故选填；措辞用「已失效」而非"失败"，避免挫败感 | 09 需求 FR-01/FR-09、09 接口 §1.1/§2/§3.7/§4 |
| A2 | 09 多轮议价让价梯度 | **本期交付只读建议**：`GET /quotes/{id}/negotiation-ladder` 返回「轮次 / 建议让价幅度 / 建议单价」；**服务端永不返回底价与剩余底线**；16 未配置 `discountLadder` 时前端隐藏该区块 | 客户砍价时销售需要**即时且有依据**的应对，这是定价能力的核心价值；「不得暴露底价」是信任与合规红线——只读 + 不下发底线可同时满足 | 09 需求 §4、09 接口 §2/§3.8/§4 |
| A3 | 10 `progress → status` | **状态由进度服务端自动推导，用户只填进度（单一录入点）**：`payment=false→pending_payment`；`payment=true 且 productionPct<100→in_production`；`productionPct=100 且 shipping=false→ready_to_ship`；`shipping=true→completed`；`shipping` 允许 `true→false` 回退纠错；状态徽标 / Tab 只读 | 让用户同时维护「进度条」和「状态」必然出现两者打架；Tab 是概览心智、进度条是明细，合并为**一个录入点**最省心且不会自相矛盾 | 10 需求 FR-05/§7.4、10 接口 §1.1/§3.3 |
| A4 | 10 `planSource` 取值 | **统一 `linear_by_time`**；`production_plan` 视为 ERP/MES 对接后的未来取值，列入后续集成专项，本期不下发 | 纯文档自相矛盾，无体验取舍；统一后接口示例可直接照抄实现 | 10 需求 §7.5、10 接口 §1.3/§3.4/§4 |
| A5 | 13 `kpiPct` 口径 | `kpiPct = min(100, round(achieved / target × 100))`，`achieved` **与 02 员工卡片同源**（当日达成值）；接口补返回 `{ metric, achieved, target, period }`，悬停展示「获客 32/35」并可下钻；未设 target → 显示「未设目标」+ 配置引导，**不显示 0%/100%** | 用户看到裸「90%」会问"90% of what"；**不可解释的指标会摧毁对 AI 数据的信任**；若与 02 卡片数字不一致，用户会立刻发现矛盾 | 13 需求 FR-05/§7.3、13 接口 §1.3/§4 |
| A6 | 13 定时自动生成报告 | **本期不做，列「后续增强 / Backlog」**；`POST /reports/generate` 为唯一入口；术语统一为「后续增强 / Backlog」，不再复用 "P1" 指代更后续 | 老板**不希望被周期性报告淹没**；自动化依赖通知推送与长任务成本配额，而手动生成已闭环，本期无用户价值增益 | 13 需求 FR-02/§7.1、13 接口 §4 |
| A7 | 00 中 14 优先级冲突 | **追认 P0 简化版入 P0 表**（列表 / 详情 / 日志 / 流式 / 重试，事实已交付）；完整版（新建 / 暂停恢复 / 转人工 / 批量）保留 P1；删除"建议列入 P1"含糊注记 | 14 是用户**唯一**能看到"AI 在干什么、卡在哪、怎么救"的入口；不可见、不可干预的黑盒是 AI 产品信任杀手 | 00 §5 P0/P1 表、§8 索引 |
| A8 | 00/16 `16 FR-07` 恢复时点 | **`FR-07` 随 09 前置（P1-S1）**；`FR-06 / FR-11` 随 P1 收口（S5）；D12 恢复时点写死 | 缺报价规则 → 运费 / 保费 / 税费与利润红线无从录入 → **AI 定价建议不可信**，09 的核心体验直接为空 | 00 §5.1 D12 |
| A9 | 15 版本号同步 | 表头补 v0.2 变更记录（正文 §7 三条已按 v0.2 澄清） | 文档卫生，避免读者误判完成度 | 15 表头 |

> 拍板后 §2 阶段划分与 §4 各模块清单可直接开工，**不再有 ⛔ 阻断项**。

---

## 8. 验收标准（Definition of Done）

单个模块 DoD：

1. 需求文档 FR 全部实现，接口字段与「页面级字段与接口文档」逐字段一致。
2. AI 行为边界满足（证据链/审批闸门/不编造/成本隔离）。
3. 前端覆盖：列表 + 详情 + 空态/加载/错误态 + 权限可见性 + 语义色。
4. 接口集成测试 + 关键单测通过；`VITE_FEATURES_PROFILE=p1` 构建通过。
5. 降级矩阵对应项恢复并回归验证。
6. §7 相关阻断项已拍板并回写文档（v1 定稿，见 §7 决策表）。

整体 P1 出口：

- [ ] 六大模块 + 16 配套全部 DoD 达成。
- [ ] `VITE_FEATURES_PROFILE=p1` 全链路 dry-run 通过（§6 P1-T-09）。
- [ ] 00 §5.1 降级矩阵 D1～D14 全部恢复或明确外推。
- [ ] 全部门禁绿，交付总结归档（对齐 [M4-交付总结.md](./server/M4-交付总结.md) 体例）。
