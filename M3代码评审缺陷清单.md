# M3 代码评审缺陷清单（调度队列 + Agent Runtime 基座）

| 项           | 内容                                                                                                                                                                                                                                                                                                           |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 版本         | v0.1（2026-09-07）                                                                                                                                                                                                                                                                                             |
| 评审对象     | 后端 M3 里程碑 · 提交 `f0f8aaa feat: M3 后端开发`（09-07 02:11，分支 `feautre-mvp`）                                                                                                                                                                                                                           |
| 对照契约     | [后端技术方案 04 任务调度与队列](./后端技术方案文档/04-任务调度与队列.md) · [05 AgentRuntime 与 LLM 网关](./后端技术方案文档/05-AgentRuntime与LLM网关.md) · [接口 14 AI 任务中心](./产品需求文档/页面级字段与接口文档/14-AI任务中心.md) · [LangGraph 00](./产品需求文档/LangGraph%20工作流/00-P0工作流定义.md) |
| 评审方式     | 静态审查 + 关键路径实读复核；**本地门禁未能复现**（node_modules 未安装、`~/Library/pnpm` 被 root 占用、沙箱无 sudo、默认 node v18 < 20）                                                                                                                                                                       |
| 收口建议顺序 | P0 → 高 → 中 → 低；测试缺口随对应修复补齐                                                                                                                                                                                                                                                                      |

分级定义：🔴 阻断（联调/功能不可用，须修复后方可进 M4）· 🟠 高（数据/契约/并发正确性）· 🟡 中（健壮性/语义偏差/缺口）· ⚪ 低（口径/风格）· 📋 测试缺口。

---

## 🔴 P0

### M3-01 POST /tasks「直投」路径任务永久卡死，并累积冻结 org 并发

- **位置**：[tasks.service.ts](server/apps/api/src/tasks/tasks.service.ts#L83-L101) ↔ [runner.ts](server/packages/runtime/src/runner.ts#L184-L200) ↔ [zombie-reaper.ts](server/apps/worker/src/scheduler/zombie-reaper.ts#L42-L46) ↔ [schema 02-ai-task.ts](server/packages/db/src/schema/02-ai-task.ts#L100)
- **现象**：员工空闲且 org<10 时，API 把任务直接落 `status='running'`（`started_at` 为 null）并在事务内即时入队；Runner 领取只认 `scheduled→running` → claim 0 行命中返回 `skipped`。
- **影响**：① 任务永久停在 `running`，无任何扫描器救起（reaper 只收割 `started_at is not null`，dispatcher 只扫 `scheduled`）；② 该行持续占用员工=1 / org=10 并发槽，**累积 10 条即冻结整个 org**；③ 入队在 withOrg 事务提交前执行，Worker 可能先于提交消费 → probe 落空同样卡死；④ `scheduledAt` 为未来 + canRun 时直投忽略延时（本应 delayed）。
- **验证**：实读确认。现有测试只以 `scheduled` 插行手动 `runner.run()`，**未覆盖 API 直投路径**。
- **建议**：统一语义——API 恒落 `scheduled`，由 claim 置 `running` 并补 `started_at`（推荐）；或 claim 兼容 `status='running' AND started_at is null` 的幂等接管。同时把 `enqueueTask` 移出事务至提交后。补一条 POST /tasks 集成测试。

---

## 🟠 高

### M3-02 retry 溯源断裂：`retry_of` 参数未落库

- **位置**：[tasks.service.ts](server/apps/api/src/tasks/tasks.service.ts#L87-L97) insert 缺 `retryOf`；[retry()](server/apps/api/src/tasks/tasks.service.ts#L309-L319) 已传参；[detail()](server/apps/api/src/tasks/tasks.service.ts#L191) 恒返回 null
- **现象**：`ai_task.retry_of` 列存在（schema L108），但 create 从未写入。
- **影响**：违反接口 14 §3.5「retry 新任务带 retry_of 溯源」，前端无法追踪重试血缘。
- **建议**：insert values 补充 `retryOf`；补 retry→detail 溯源测试。

### M3-03 SSE 终态丢失窗口：开流只读一次状态快照，完成落在订阅前则流永不结束

- **位置**：[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L51-L64) 快照读取早于 [:103](server/apps/api/src/tasks/task-stream.controller.ts#L103) 订阅；done/status 事件不入库、回放仅覆盖 `ai_task_log`（L106-131）
- **现象**：任务在「读快照(running)」与「订阅成功」之间提交终态并 PUBLISH → done 事件丢失 + 快照仍 running → 客户端挂等（心跳无限延续）。
- **建议**：订阅成功后、写快照前重读一次任务行；以 DB 最新态为准补发 status+done 并关闭；或在 API 侧加终态兜底轮询。

### M3-04 FollowUpScanner 多 worker 实例可重复建 ai_task（无行锁、无唯一约束兜底）

- **位置**：[follow-up-scanner.ts](server/apps/worker/src/scheduler/follow-up-scanner.ts#L75-L91) 预检为独立批量快照；insert（L227-236）前不锁 `follow_up_task` 行
- **影响**：两个 worker 实例交错均可通过预检各自插入，违反文档 04 §3.1「多实例部署安全」（Dispatcher 侧有 claim 兜底，scanner 侧无）。
- **建议**：为 `followUpTaskId` 建列 + 部分唯一索引，或按文档实现 `FOR UPDATE SKIP LOCKED` 领取。

### M3-05 email_send 发送前新鲜度语义过宽：任何历史 in 邮件都阻断 resume

- **位置**：[crm-tools.ts](server/packages/tools/src/builtin/crm-tools.ts#L314-L329) 条件仅 `direction='in'`（+reply 模式排除触发消息），无时间边界
- **影响**：reply 模式下触发消息之前的来信也判 `fresh=false`；follow_up 模式下会话中出现过任何 in 消息即拒发，与「批准后、发送前检查客户是否有新增回复」意图不符。
- **建议**：reply 按「新于触发消息 createdAt」过滤；follow_up 按「新于最近一次 outbound」过滤，与 [flows.ts](server/packages/workflows/src/flows.ts) check_replied 口径一致。

### M3-06 ✅ 已修复（2026-09-07）resume 无员工并发复核，同员工可双 running；complete/fail 无条件回写员工 idle 互相覆盖

- **位置**：[dispatcher.ts](server/apps/worker/src/scheduler/dispatcher.ts#L75-L78) waiting_approval 不计并发（注释）↔ [runner.ts](server/packages/runtime/src/runner.ts#L157-L181) resume 直接把 waiting_approval→running 并置员工 working，不校验并发；[complete/fail](server/packages/runtime/src/runner.ts#L291-L315) 无条件回 idle
- **影响**：员工持有 pending 审批时会被派发第二任务 → 双 running；任务 A 未结束员工已 idle；员工状态互相覆盖。
- **修复结论（采纳「挂起占用员工位」口径）**：① 员工占用口径统一为 `running` + `waiting_approval`（shared `EMPLOYEE_OCCUPYING_TASK_STATUSES`），Dispatcher 员工闸门 / [tasks.service create](server/apps/api/src/tasks/tasks.service.ts#L79-L93) 直投判定 / [DelayedJobReconciler 补投](server/apps/worker/src/scheduler/delayed-reconciler.ts) 三处同口径，源头杜绝「挂起员工被派第二任务」；org 10 并发仍仅统计 running（挂起不占 org 额度，语义不变）。② 终态回写前置校验：[releaseEmployeeIdle](server/packages/runtime/src/release-employee.ts)（无其它占用任务才回 idle），runner.complete/fail、ApprovalExpiryScanner 超时级联、ZombieReaper 收割三处接入，防「A 结束把跑 B 的员工误置 idle」。③ 测试：scheduler.integration.spec 更新 Dispatcher 员工闸门断言（waiting_approval 占位不投）+ 新增 releaseEmployeeIdle 两用例。④ 文档同步：后端 04 §2/§3.1/§3.3/§5.1、Runtime 总纲 §4.1/§4.7、接口 14 §3.2/§4。resume 路径在正常调度下不可能撞上员工 busy（源头已堵）；残留的 Dispatcher 多实例/直投并发提交窗口竞态需 claim 层行锁强化，建议随 M4 处置接口实装评估。
- **建议**：需产品明确「挂起不占并发」口径下的强约束方案——resume 时对员工并发做一次预检（不满足则保持 waiting_approval），或让 waiting_approval 计入员工占用；终态回写加状态前置校验。

---

## 🟡 中

### M3-07 ✅ 已修复（2026-09-07）LLM 结构化输出：未知键被 zod 静默剥离、重试不回喂错误（违反项目硬约束）

- **位置**：[llm-gateway.ts](server/packages/runtime/src/llm-gateway.ts#L138-L184) 两次重试用相同 messages；[output-schemas.ts](server/packages/workflows/src/output-schemas.ts) 无 `.strict()`
- **影响**：M3 mock provider 影响有限；M4 接真实 LLM 前应补齐——高风险 schema 用 `.strict()`/refine 拒绝未知键，并把校验错误回喂重试（对齐历史 P1 教训）。
- **修复结论（2026-09-07）**：① [output-schemas.ts](server/packages/workflows/src/output-schemas.ts) 全量输出 schema 加 `.strict()`（含 leadScore.reasons 嵌套对象），未知键从「zod 静默剥离」改为「校验失败」；② [llm-gateway.ts](server/packages/runtime/src/llm-gateway.ts#L138-L171) `structured()` 引入 retryHint：校验失败 issues / JSON 解析失败原因回喂下一次请求的 user 消息，自纠正重试而非同参盲试。M3 mock 产出严格符合 schema，路径不变；M4 真 LLM 联调即生效。

### M3-08 ✅ 已修复（2026-09-07）节点级 risk 与工具级 riskLevel 双轨，门控只认后者

- **位置**：[workflow-state.ts](server/packages/shared/src/workflow-state.ts#L46-L58) 声明节点 risk 触发 Gate；SOP 写 `risk:'medium'`（[sops.ts](server/packages/workflows/src/sops.ts#L60)）；但 [compiler.ts](server/packages/runtime/src/compiler.ts#L410) 只用注册表 `tool.riskLevel`
- **修复结论（2026-09-07）**：[compiler.ts](server/packages/runtime/src/compiler.ts#L402-L413) `toolExec` 门控口径统一为 `node.risk ?? tool.riskLevel`——SOP 节点标注的 risk 优先（对同一工具可实例化抬高/放行），缺省回落工具注册表 riskLevel；GateToolMeta.riskLevel 与 approval_request.riskLevel 同步使用该值。与既有 `node.approvalType ?? tool.approvalType` 口径一致，SOP 标注与实现不再漂移；总纲 §4.7（节点 risk 触发门控）与 §4.5（工具默认级）语义同时成立，无需改文档。
- **建议**：后续新增工具若默认 low、个别 SOP 需审，直接在节点标注 risk 即可。

### M3-09 ✅ 已修复（2026-09-07）SSE 重放与实时流未按 logId/seq 去重

- **位置**：[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L103-L131)
- **现象**：先订阅后回放之间 PUBLISH 的事件会被回放查询与订阅回调双收，代码未去重（文档 04 §6.1 要求按 seq 去重）。
- **修复结论（2026-09-07）**：[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L80-L104) 新增 `sentLogIds` + `sendLog` 统一出口——回放与实时 log 事件均以 `ai_task_log.logId` 为唯一键登记，先到者发出、后到者丢弃；两种交错方向（实时先到→回放兜底再查库 / 回放先发→实时 PUBLISH 重复）都被覆盖。

### M3-10 ✅ 已修复（2026-09-07）SSE 连接计数在 subscribe 抛错路径泄漏

- **位置**：[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L74) 先 `++count`，subscribe（L103）抛错时既不入 try 也不递减
- **修复结论（2026-09-07）**：[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L105-L113) `subscribe` 纳入 try/catch，抛错路径调用 `cleanup`（递减连接计数 + unsubscribe/quit + res.end）后 return，计数不再泄漏。

### M3-11 ✅ 已修复（2026-09-07）commit→PUBLISH 崩溃丢终态事件：outbox 取舍文档化 + API 侧终态兜底轮询

- **位置**：[compiler.ts](server/packages/runtime/src/compiler.ts#L543-L547) flush 在事务提交后 ↔ [task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L178-L211)（M3-11 兜底轮询）↔ [04 §6.3](后端技术方案文档/04-任务调度与队列.md)
- **影响**：Worker 在 commit 与 PUBLISH 之间崩溃则进度/终态事件永久丢失；回放仅补 log 不补 status/done。
- **修复结论（2026-09-07）**：事件通道维持「先写库、后 PUBLISH」无事务 outbox——取舍写入文档 04 §6.3 + Runtime 总纲 §4.9（DB 为权威源、Redis 事件尽力实时，不承担送达保证）。丢失窗口三层兜底闭环：① 开流后重读 DB（M3-03）覆盖「读快照→订阅成功」窗口；② 新增 API 侧终态兜底轮询——[task-stream.controller.ts](server/apps/api/src/tasks/task-stream.controller.ts#L178-L211) 对非终态连接每 5s 重读任务行，DB 已终态且本连接未发过 done（实时 done 到达即置位停表）→ 按 DB 最新态补发 status+done 并关闭，弥合「连接存活期间 commit→PUBLISH 崩溃」窗口；③ 前端 /tasks 轮询兜底（既有）。
- **测试**：SSE API 层终态补发/去重/断线/连接上限并入 M3-T4 统一补齐。
- **建议**：M4+ 若通知 / Webhook（06 §5）要求「至少一次」投递，再评估事务 outbox（事件表 + 投递游标）或 BullMQ job 承载事件。

### M3-12 ✅ 已修复（2026-09-07）q:notify / q:email_sync 复用 ai_task processor，无分流即被静默丢弃

- **位置**：[processor.ts](server/apps/worker/src/queues/processor.ts#L23-L63)（按队列分流）+ [registry.ts](server/apps/worker/src/queues/registry.ts) 注册全量队列 + [approval-expiry.ts](server/apps/worker/src/scheduler/approval-expiry.ts#L154) 实际投递 q:notify
- **影响**：processor 不按队列路由——系统队列 job（job.id 非 ai_task.id）误入 TaskRunner 被 missing/skipped 静默吞载荷；M4/M5 接入真实消费时是坑。
- **修复结论（2026-09-07）**：[processor.ts](server/apps/worker/src/queues/processor.ts) `createProcessor` 按 `job.queueName` 分流：task_type 队列（`TASK_TYPE_QUEUE` 值域，job.id=taskId）→ TaskRunner 执行 + resume 透传；系统队列 → `handleSystemJob` 独立处理器——q:notify 消费即 ack + info 留痕（发送端随 M5 #11），q:email_sync warn 显式留痕（M3 无生产者，M4 #4 接入），绝不落入 TaskRunner。显式「丢弃留痕」替代「静默吞」，接入真实消费时替换分支即可、拓扑不变。新增单测 [queue-processor.spec.ts](server/apps/worker/test/queue-processor.spec.ts)（4 用例：task 执行 / resume 透传 / 非合法 resume / 系统队列不触 runner）通过；typecheck/eslint/prettier 全绿。
- **建议**：M5 #11 通知服务实装时挂 q:notify 真实分发 handler；approval_expired 提醒依赖 approval_log「防重不可重发」的口径需在 M5 重评（M3/M4 期通知不送达为显式取舍）。

### M3-13 ✅ 已修复（2026-09-07）email_sync 调度与真实额度重置缺失（M3 #5 未闭环，排 M4 前置）

- **位置**：`scheduler/` 无 MailboxSync 扫描器；[quota-reset.ts](server/apps/worker/src/scheduler/quota-reset.ts#L1-L17) 为 24h 巡检占位（注释自述 M4 实装）
- **修复结论（2026-09-07，按评审建议排期并注明 M3 出口）**：M3 收信链路与外部配额消耗未实装、无投递源，不提前实现——维持「quota key 按日分片 + TTL 48h 天然轮换、代码仅巡检留痕」口径。出口已注明：[后端技术方案 04 §3.1](后端技术方案文档/04-任务调度与队列.md)（MailboxSyncScheduler → M4 #4、QuotaReset 按 org 时区日界精确重置 → M4 #6）+ [后端开发计划表](后端开发计划表.md)（M3 #5 标注部分、M4 #4 收信链路含周期触发投递、M4 #6 承接精确重置）。q:email_sync 消费侧已随 M3-12 分流处理器就位（M3 收到即 warn 显式留痕，防静默吞）。
- **建议**：M4 邮箱驱动联调时按 04 §3.1 排 MailboxSyncScheduler；M4 #6 配额真实化时实现 QuotaReset 按 org 时区日界精确重置（16 设置口径先行确认）。

### M3-14 ✅ 已修复（2026-09-07）审批 TTL 硬编码常量，未接 16 设置按类型可配

- **位置**：[approval-gate.ts](server/packages/runtime/src/approval-gate.ts#L20-L30)（原 L21 硬编码常量）↔ [schema 01-org-user.ts](server/packages/db/src/schema/01-org-user.ts#L92-L98) ↔ [context.ts](server/packages/runtime/src/context.ts#L35) ↔ [runner.ts](server/packages/runtime/src/runner.ts#L259-L285)
- **修复结论（2026-09-07，采纳「按审批类型可配」口径，12 §7.2）**：① [schema 01-org-user.ts](server/packages/db/src/schema/01-org-user.ts#L92-L98) `ApprovalRule` 增加可选 `expireHours`（小时）——挂在既有 `role_permission.approval_rules` JSONB 行内，**无 DDL 迁移**；② [runner.ts](server/packages/runtime/src/runner.ts#L259-L285) `loadSnapshot` 把匹配员工角色的规则解析为 `approvalTtlMsByType: Record<approvalType, ms>` 写入 org 快照；③ [context.ts](server/packages/runtime/src/context.ts#L35) `OrgRuntime` 新增 `approvalTtlMsByType` 字段；④ [approval-gate.ts](server/packages/runtime/src/approval-gate.ts#L27-L30) 新增纯函数 `approvalTtlMs(ctx, tool)`：按 `approvalType ?? tool.name` 查配置、缺省/非法回落 `APPROVAL_TTL_MS=48h`，`enterWaiting` 与 `recordAutoApprove` 创建审批单的 `expiresAt` 统一走该 TTL；⑤ [settings.dto.ts](server/apps/api/src/settings/settings.dto.ts#L72) `rolePermissionsSchema.approvalRules` 加 `expireHours: z.number().int().min(1).max(720).optional()`，settings.service `assertApprovalRules` 同步（范围校验由 DTO zod 层完成）。⑥ **测试**：approval-gate.test 新增「按类型 TTL」用例（未配置回落 48h / 命中 approvalType / 按 tool.name 兜底 / ≤0 非法回落）；org-settings.integration.spec 断言 `expireHours:6` 持久化返回 + DTO 拒绝 0/721。⑦ **文档**：16 产品 §2.7（行内「超时 [48]h」）、接口 16 §1.7/§3.6 同步（默认 48、范围 1~720 整数）。ApprovalExpiryScanner 侧消费 DB `expires_at`，口径不变。

### M3-15 ✅ 已修复（2026-09-07）LLM 预算检查语义与文档偏差

- **位置**：[llm-gateway.ts](server/packages/runtime/src/llm-gateway.ts#L96-L106)（crossedBudget）↔ record 预算段 [L269-L276](server/packages/runtime/src/llm-gateway.ts#L269-L276) ↔ [worker index.ts](server/apps/worker/src/index.ts#L52-L60)（alert 接线）
- **修复结论（2026-09-07，采纳「MVP 增量口径」，10min 汇总 + 80%/100% 两级随 M5 #9 复核）**：① [llm-gateway.ts](server/packages/runtime/src/llm-gateway.ts#L96-L106) 新增导出纯函数 `crossedBudget(prevTotalUsd, nextTotalUsd, budgetUsd)`——仅「前值 ≤ 预算且当前 > 预算」为 true，持续超限不重复上报；`record()` 预算段改为「调用时当月累计 + 跨 100% 阈值首超才告警」，替代每次 `logger.warn`。② `GatewayOptions` 新增可选回调 `alert?: (info: BudgetAlertInfo)`，导出载荷 `BudgetAlertInfo`（orgId/scene/totalUsd/budgetUsd，金额以 string 携带防精度）；不接回调时静默、**不熔断**。③ [worker index.ts](server/apps/worker/src/index.ts#L52-L60) 装配 `LlmGateway({ ..., alert })` → `TaskEnqueuer.enqueueNotify({ type:'budget_limit', ...info })` 入 q:notify 留痕（真实通知分发随 M5 #11，先入队防静默吞）。④ **测试**：llm-gateway.test 新增 crossedBudget 五用例（首次越界 true / 恰好等于预算 false / 持续超限不重复 false / 预算 0 首笔告警 / 负前值防御）。⑤ **文档**：后端 05 §6.4、08 §6 成本失控行、09 观测表同步 MVP 口径并标注 M5 #9。
- **建议**：M5 #9 预算 Cron（10min 汇总 80%/100% 两级）复核时叠加两级告警；q:notify 真实分发实装后由 M3-12 系统队列 handler 落站内/邮件通知。

### M3-16 ✅ 已修复（2026-09-07）cost_price 敏感字段排除缺失

- **位置**：[prompt-guard.ts](server/packages/shared/src/prompt-guard.ts)（新建）↔ [compiler.ts](server/packages/runtime/src/compiler.ts#L581)（renderTemplate 出口）↔ [flows.ts](server/packages/workflows/src/flows.ts#L252)（loadThread 客户来信）
- **修复结论（2026-09-07，采纳「建统一守卫并接出口」口径）**：① 新增 [prompt-guard.ts](server/packages/shared/src/prompt-guard.ts)：`PROMPT_SENSITIVE_KEYS`（costPrice/cost_price/purchasePrice/purchase_price/purchase_cost/unitCost/unit_cost/采购成本，purchase_cost 与 purchase_price 别名同口径）、`stripSensitiveFields(value, extraDenyKeys?)`（递归剥离敏感键、返回副本不改原入参）、`boundExternal(content)`（不可信内容包 `[UNTRUSTED_EXTERNAL_BEGIN/END]` 边界标记），shared index 导出。② LLM prompt 组装**唯一出口** [compiler.renderTemplate](server/packages/runtime/src/compiler.ts#L581)：非字符串变量统一 `JSON.stringify(stripSensitiveFields(value))`，对象变量进 prompt 前剥敏感键，兜底防 `cost_price` 等经结构化变量泄漏。③ [flows.loadThread](server/packages/workflows/src/flows.ts#L252)：`direction='in'` 客户来信（不可信外部内容）进 prompt 前统一 `boundExternal` 包边界（与 M3-05 同源外部数据纪律一致）。④ **测试**：prompt-guard.test 新增 stripSensitiveFields（嵌套对象/数组剥离、不改原入参、自定义 deny、标量透传）+ boundExternal + renderTemplate 出口兜底用例。⑤ **文档**：后端 08 §6 提示注入 / 敏感数据泄漏两行同步守卫机制；schema 注释与知识生成剔除（07 §6）不变。RLS/接口层敏感拦截在 M4 RAG 文档级裁剪时复用同一守卫。

---

## ⚪ 低 / 文档口径

### M3-17 语义简化项与契约文档偏差（建议统一为文档口径，随各模块收口对齐）

1. 原生 `interrupt()`/`Command{resume}` 未用，改为「Gate 抛 ApprovalPendingError → runner 收尾 → resume job 重入 invoke(null) 续跑」——机制闭环，但 [05 §2/§4.2](后端技术方案文档/05-AgentRuntime与LLM网关.md) 需同步更新。
2. SOP 编译缓存键为 `taskType@version`（[compiler.ts](server/packages/runtime/src/compiler.ts#L226)），文档为 `(sopId,version)`；org 自定义 sop_template 未读 DB（随 P1）。缓存键需含 org 维度，否则多租户交叉。
3. LLM 路由 scene=taskType（[compiler.ts](server/packages/runtime/src/compiler.ts#L374-L385)），未接「promptRef→档位映射 + high→medium 降级链」。
4. SSE 上限 5/用户、仅进程内计数、无 org 维度（文档 10/用户、org 200 + 42901）。
5. 队列名用 `q.` 前缀（BullMQ 禁 `:`，注释说明充分，合理偏差）。
6. Memory org memory 回流为空实现（[memory.ts](server/packages/runtime/src/memory.ts#L139-L145)，自述 P1）。
7. `ai_task.status` DB 默认值 `'running'`（[schema](server/packages/db/src/schema/02-ai-task.ts#L100)）易误导未来误插入 running 无 started_at 行，建议改 `'scheduled'` 或加 CHECK。
8. BullMQ delayed job / removeTask（[enqueue.ts](server/packages/runtime/src/enqueue.ts#L47-L66)）全仓无调用方；实际排期=DB `scheduled_at` + Dispatcher 到期投递 + [delayed-reconciler.ts](server/apps/worker/src/scheduler/delayed-reconciler.ts) 兜底 → 文档 04 §3.4 口径需更新。

---

## 📋 测试缺口（M3 出口标准复核）

| ID    | 缺口                                                                                                                                                                                                | 建议                                     |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| M3-T1 | **断线 after 增量补齐无服务端测试**（出口标准第 4 条未覆盖；api/test 仅有 org-settings）                                                                                                            | 补 logs?after= 与 SSE 断线重连用例       |
| M3-T2 | ApprovalExpiryScanner / ZombieReaper / DelayedJobReconciler 无 spec                                                                                                                                 | 审批超时级联、心跳收割、对账补投各补用例 |
| M3-T3 | tasks 接口无 api 层测试（POST 直投、retry_of 溯源、入队判定、越权）                                                                                                                                 | 覆盖 M3-01/M3-02                         |
| M3-T4 | SSE API 层无测试（去重、终态补发、连接上限）                                                                                                                                                        | 覆盖 M3-03/M3-09/M3-10                   |
| M3-T5 | 预算告警（M3-15）无测试 — ✅ 已闭环（2026-09-07）：crossedBudget 纯函数五用例（首次越界 / 恰好等于预算 / 持续超限不重复 / 预算 0 / 负前值防御，llm-gateway.test）；「不熔断」由调用方不阻断语义保障 | 接真实 LLM 后补 gateway/DB 集成断言      |

---

## 整体结论

- **进度**：🟡 中档 M3-06~16 十一项全部修复闭环；待处理剩 🔴 M3-01（API 直投卡死 + 测试盲区，唯一阻断）与 🟠 M3-02~05（数据/契约正确性，须 M4 前收口）及 ⚪ M3-17（文档口径，随各模块收口对齐）；计划表「M3 未开始」状态需更新。
- **质量**：幂等/RLS/事件时序/审批分流等设计质量高，测试资产扎实，骨架达可进 M4 门槛；**唯一阻断为 M3-01（API 直投卡死 + 测试盲区）**，建议修复 + 补 API 集成测试后再进入 M4，前端联调 POST /tasks 会立即暴露。
- **验证限制**：评审时沙箱无法运行门禁（依赖未装 + pnpm 目录权限 + node 版本）。M3-14~16 修复后补验：typecheck 全包绿（shared/db/runtime/workflows/worker/api，node v18 下执行）；runtime 单测 26/26 通过（approval-gate 12 + prompt-guard 8 + llm-gateway 6）；新增/改动文件 eslint 通过。**遗留**：① 仓库未提交的存量改动（runner/compiler/approval-gate 类型导入风格、worker index closeDb 未用等 7 处 eslint 告警）不在本次 diff 范围；② org-settings.integration.spec（含 expireHours 持久化/DTO 拒绝断言）本地未执行——5432 被无关 rag-postgres 占用、tradepilot 角色未建，待 docker compose 环境就绪后跑 `pnpm --filter @tradepilot/db migrate` + 该 spec。
