# TradePilot AI · P1 开发进度总览

> 核对时间：2026-09-13（P1 计划起始日为 2026-09-14）
> 依据：[P1-开发需求清单.md](./P1-开发需求清单.md)、[P1-开发任务计划表.md](./P1-开发任务计划表.md) 与代码实际实现
> 结论：**S1（横切底座）全部完成** —— S1-A（定价引擎 + 16 FR-07 + 数据层校核）与 S1-B（P1-X-20~22 通知/Webhook/开放 API + **P1-X-30~33 任务中心操作 pause/resume/cancel/转人工/失败批量** + P1-X-40~43 分析预聚合 ETL / 指标口径 / `savedHours` / 归因）均交付，且 **模块 16 设置全部 5 项交付完毕（P1-16-01~05 ✅，含前端 pricing-rules / api-keys / crm-integration 三个正式页面）**；**S2 已交付 08 产品中心（P1-08-01~15，见 §4）、09 报价中心（P1-09-01~13，见 §8）与 10 订单中心（P1-10-01~16，见 §9）**（08 图以新 `task_type=product_knowledge` 落地）；`manager/analytics` 路由、worker `business_analysis` 队列、前端 `/manager /data-center`（仍 `PlaceholderView`）尚未开始

| 模块 / 能力 | 需要开发的功能 | 任务编号 | 任务数 | 是否完成 |
|---|---|---|---|---|
| 16 设置（P1 配套） | FR-07 产品与报价规则、FR-06 CRM 集成、FR-11 API/Webhook、设置页替换 | P1-16-01~05 | 5 | ✅ 已完成（P1-16-01~05 全交付；CRM 集成外呼部分按 06 §6 留 `CrmDriver`） |
| 横切·定价引擎 | 五项成本、利润红线、汇率快照、cost_snapshot、reasons[]、单测 | P1-X-10~15 | 6 | ✅ 已完成 |
| 横切·通知/Webhook | q.notify 实装、Webhook 出站、开放 API 中间件 | P1-X-20~22 | 3 | ✅ 已完成 |
| 横切·任务中心操作 | pause/resume/cancel、转人工、失败批量处理 | P1-X-30~33 | 4 | ✅ 已完成（状态机 + 员工位/SSE/delayed job 联动；resume 从检查点续跑） |
| 横切·分析预聚合 | 定时 ETL、指标口径、savedHours 常量、归因 | P1-X-40~43 | 4 | ✅ 已完成（org×当地日 ETL + 员工级归因；数据中心 REST 接口随 15 P1-15-xx 接入） |
| 08 产品中心 | 产品 CRUD、资料上传、AI 分析/知识生成、列表/详情/表单页 | P1-08-01~15 | 15 | ✅ 已完成（P1-08-01~15 全交付；08 图以新 `task_type=product_knowledge` 落地，见 §4） |
| 09 报价中心 | 报价 CRUD、AI 定价、提交/审批/外发、won、PDF、lost/议价、前端 | P1-09-01~13 | 13 | ✅ 已完成（P1-09-01~13 全交付，含 §7-A1 lost 闭环与 §7-A2 只读议价梯度，见 §8） |
| 10 订单中心 | 建单/转单、进度、风险与执行、变更审批、监控图、前端 | P1-10-01~16 | 16 | ✅ 已完成（P1-10-01~16 全交付；`order_change` 变更走高危审批，见 §9） |
| 14 AI 任务中心（完整版） | 状态 Tab、新建任务、暂停/恢复/转人工、失败批量、详情 | P1-14-01~06 | 6 | ❌ 未完成 |
| 15 数据中心 | 趋势/分布/贡献/下钻/导出接口、数据权限、图表前端 | P1-15-01~10 | 10 | ❌ 未完成 |
| 13 AI 外贸经理 | overview、discoveries 与执行、团队效率、报告生成、前端 | P1-13-01~13 | 13 | ❌ 未完成 |
| 02 卡片启用 + 降级矩阵 | 跟单/经理卡片、FR-04 完整任务列表、KPI 口径、D1~D14 恢复 | P1-02-01~04、D1~D14 | 18 | ❌ 未完成 |
| 数据层校核 | 表/枚举/字段/RLS 迁移校核 | P1-DB-01~04 | 4 | ✅ 已完成 |
| 测试与门禁 | 单测/集成/降级回归/门禁/dry-run | P1-T-01~09 | 9 | ❌ 未完成 |
| **合计** | — | — | **127** | **70/127 完成（≈55.1%，≈92.5 人天待投入）** |

---

## 1. 本轮完成明细（S1-A · 定价引擎 + 16 FR-07 + 数据层校核）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-X-10 | 五项成本核算 `computeUnitCosts`（`purchase/freight/insurance/tax/fx`，支持 16 `costItems` 启停与行级费率覆盖） | `server/packages/core/src/pricing.ts` |
| P1-X-11 | `computeProfitMarginPct` / `computeQuoteProfit` / `assertProfitFloor`（低于红线抛 `BIZ_VALIDATION`=42201，BigInt 定标无浮点误差） | 同上 |
| P1-X-12 | `buildExchangeRateSnapshot` 三元组 `{rate,date,source}`（rate 定标 8 位，date 强校验 `YYYY-MM-DD`，默认 `manual`） | 同上 |
| P1-X-13 | `mergeCostSnapshot`（引擎结果 + 人工覆盖，覆盖优先）/ `aggregateCostBreakdown`（行级 × 数量 → 报价级汇总） | 同上 |
| P1-X-14 | `buildPricingReasons`（数量档位 / 客户评分 / 历史成交，对齐 Insight Schema，无证据不产出） | 同上 |
| P1-X-15 | 定价引擎单测（五项成本、红线拦截、快照不变、汇率快照、议价梯度） | `server/packages/core/test/pricing.test.ts`（`pnpm --filter @tradepilot/core test` 全绿） |
| P1-16-01 | org 单例 `pricing_rule_setting`：DTO（成本项/贸易条款白名单 + 红线/币种/汇率源强校验）、首访惰性落默认、保存即生效 | `server/apps/api/src/settings/settings.dto.ts`、`settings.service.ts` |
| P1-16-05（部分） | `GET/PUT /settings/pricing-rules`（读 admin+manager、写仅 admin；字段与接口文档 16 §1.6 逐字段一致） | `server/apps/api/src/settings/settings.controller.ts` |
| P1-DB-01 | 校核 14 张 P1 表已在 `0000_black_shocker.sql` 落库（产品 7 / 报价订单 6 / `analytics_daily_summary`） | drizzle 迁移产物 |
| P1-DB-02 | 补 `0012_manual_p1_check_constraints.sql`（12 项 ER 区间类 CHECK：`min_qty/quantity/production_pct/confidence/match_pct/score/purchase_probability` 等，幂等 `pg_constraint` 存在性检查） | 已执行 `pnpm --filter @tradepilot/db migrate` 并核验 17 项 `ck_*` 约束在库 |
| P1-DB-03 | 枚举校核：`quote_status`（含 `lost`）/`order_status`/`order_risk`/`product_status`/`report_period`/`report_status`/`discovery_type`/`product_doc_type` 与 ER 一致；1 处已文档化偏差（`msg_status` 多 `waiting_approval`，由 `manual/0007` 幂等补齐） | `server/packages/db/src/schema/enums.ts` |
| P1-DB-04 | schema ↔ ER 逐字段比对：`cost_snapshot jsonb DEFAULT '{}'`、`deleted_at/deleted_by`、`quotation.approval_id` FK 等一致；唯一系统性缺口即上表 CHECK，已补齐 | — |

## 2. 本轮完成明细（S1-B · 通知/Webhook/开放 API，P1-X-20~22）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-X-20 | `q.notify` 发送端（M5-A2 已实装，本轮补齐出站派生）：site 站内落库 + email 走 org connected mailbox（无邮箱降级仅站内） | `server/apps/worker/src/queues/notify.ts` |
| P1-X-21（契约） | 事件注册表 `WEBHOOK_EVENT`（16 FR-09 三项 + 06 §5.2 四项业务事件）、`WebhookPayload`/`WebhookJob` zod 契约、`webhookMatchesEvent`（命中原始 type 或归并事件键） | `server/packages/shared/src/contracts/open-api.ts` |
| P1-X-21（订阅） | `GET/POST/DELETE /settings/webhooks`：`{ url, events[], secret }`；secret 以 AES-256-GCM 入库（`secret_enc`），列表/响应永不回显；仅 admin | `server/apps/api/src/open-api/webhook.service.ts`、`open-api.controller.ts` |
| P1-X-21（投递） | notify 命中 active 订阅 → 派生 `q:webhook`（job 级 attempts=5 + 指数退避 1m 起）；投递 POST JSON、10s 超时、`X-TP-Signature: t=…, v1=HMAC-SHA256(secret, t.body)`；末次失败打 `deadLetter` 标记 | `server/packages/integrations/src/webhook-out/index.ts`、`server/apps/worker/src/queues/webhook.ts`、`processor.ts`、`index.ts` |
| P1-X-22（密钥） | `GET/POST/DELETE /settings/api-keys`：`tpk_live_{8hex}{base64url}` 明文仅创建时返回一次、库存 sha256（`key_hash`）、`key_prefix` 供列表识别；撤销即时失效 | `server/apps/api/src/open-api/api-key.service.ts` |
| P1-X-22（鉴权） | `x-api-key` 分支并入 `JwtAuthGuard`（0 信任 JWT，回填 `request.apiKeyAuth` + ALS）；`ScopesGuard`（API Key 通道 fail-closed：未声明 `@Scopes` 拒绝、缺 scope → 40301）；每 Key 固定窗口 60 req/min → 42901；`last_used_at` 记账 | `server/apps/api/src/auth/guards/jwt-auth.guard.ts`、`guards/scopes.guard.ts`、`decorators/scopes.decorator.ts` |
| P1-DB-02（补） | `0013_manual_api_key_lookup.sql`：`api_key` 追加 `apikey_lookup`（仅 SELECT、`app.apikey='1'` 放行）——入站鉴权时 org 未知，无法用 `withOrg` 打开上下文 | 已执行 migrate 并核验策略在库（`apikey_lookup | cmd=r` + `tenant_isolation`，`rls=true force=true`） |
| P1-T（新增单测） | Webhook 签名/POST 契约/2xx 判定/异常归一（独立 HMAC 实现比对）；事件注册表与载荷契约 | `server/packages/integrations/test/webhook-out.spec.ts`（4 例）、`server/packages/shared/test/open-api.test.ts`（6 例） |

## 3. 本轮完成明细（16 设置 · P1-16-02~05 收口）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-16-03（前端） | `ApiKeysView.vue`：API Key 列表（名称/前缀/scope/状态/最近使用）+ 创建弹窗（scope 白名单勾选）+ **明文一次性展示弹窗**（复制 + 关闭即不可再查看）+ 撤销；Webhook 列表（地址/事件/状态）+ 新增弹窗（URL 校验、事件白名单勾选、secret ≥16 位并支持随机生成）+ 删除 | `web/src/features/settings/views/ApiKeysView.vue` |
| P1-16-01/04（前端） | `PricingRulesView.vue`：产品分类（tag 录入）、成本项（五项勾选，至少 1 项）、利润红线、让价梯度（动态档位 1~100 整数）、默认贸易条款 / 币种、汇率源只读；仅 admin 可写、非 admin 只读降级 | `web/src/features/settings/views/PricingRulesView.vue` |
| P1-16-02（后端） | CRM 集成配置层：`GET/POST/PUT/DELETE /settings/integrations`；`provider ∈ {xiaoman,futong}`、`syncDirection ∈ {pull,push,both}`、`status ∈ {connected,disconnected}`；同供应商每 org 唯一（服务层拦截 → 40901）；`mapping` 白名单化（05 CRM 口径 9 字段，禁重复映射）写入 `crm_integration.mapping`（jsonb，`$type` 仅类型标注、无 DDL 变更）；读取 admin+manager / 写入仅 admin | `server/apps/api/src/settings/settings.{dto,service,controller}.ts`、`server/packages/db/src/schema/01-org-user.ts` |
| P1-16-02（前端） | `CrmIntegrationView.vue`：集成列表（供应商/状态/同步方向/映射条数/最近同步）+ 授权弹窗（供应商去重、方向 radio、映射动态行「本地字段 → CRM 字段名」）+ 编辑 + 启用/停用 + 断开；依据 06 §6「MVP 不做任何外呼」，页面明确标注同步由后续 `CrmDriver` 按 `syncDirection` 执行 | `web/src/features/settings/views/CrmIntegrationView.vue` |
| P1-16-04（接线） | 三处设置页全部由 `SettingsPlaceholderView` 换为正式页面；`crm-integration` / `pricing-rules` 收紧为 `roles: ['admin','manager']`，`api-keys` 为 `['admin']`；删除占位页与 `SettingsLayout` 的 `p1`「即将上线」标记 | `web/src/router/routes/modules.ts`、`SettingsLayout.vue`（`SettingsPlaceholderView.vue` 已删除） |
| 契约与 API 层 | 类型：`PricingRules`、`ApiKey`/`ApiKeyCreated`、`Webhook`、`CrmIntegration`/`CrmFieldMapping`，及与后端同源的 `PRICING_COST_ITEM_KEYS` / `INCOTERMS_OPTIONS` / `API_SCOPES` / `WEBHOOK_EVENTS` / `CRM_PROVIDERS` / `CRM_SYNC_DIRECTIONS` / `CRM_LOCAL_FIELDS` 白名单；请求封装 12 个函数 | `web/src/api/types/settings.ts`、`api/resources/settings.ts` |
| i18n | settings 命名空间新增 FR-07 / FR-11 / FR-06 全部文案（zh-CN + en，含 scope、webhook 事件、CRM 供应商/方向/字段标签） | `web/src/locales/zh-CN.ts`、`locales/en.ts` |
| 校验 | 后端 `tsc -p apps/api` + `eslint`；前端 `vue-tsc`、`eslint`、`stylelint`、`vitest run tests/unit`（13 文件 / 73 例）全绿 | — |

## 4. 本轮完成明细（S2 · 08 产品中心，P1-08-01~15）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-08-01/03/04 | `GET /products`（keyword=名称/SKU + category/status，分页）、`GET /products/{id}`（Overview/Specifications/Pricing/Documents/AI Knowledge 全页签）、`PUT /products/{id}`（字段可选 + 规格/阶梯价整体替换） | `server/apps/api/src/products/products.{controller,service}.ts`、`products.dto.ts` |
| P1-08-02 | 新增产品：SKU 同租户唯一 → `42201`（依赖 `uq_product_sku`，服务层不吞库错误）；阶梯起始数量重复前置校验；插入基础字段 + 规格 + 阶梯价同事务 | 同上 |
| P1-08-05 | 资料上传/删除：multipart `file`+`docType`（四类白名单 pdf/docx/md/txt + 50MB + **magic number**）→ 对象存储 `pdoc/{orgId}/{fileId}.{ext}` → 事务内建 `product_document` + 归档 `knowledge_document(source='product', category='product', status='indexing')` → 提交后入队 `q:knowledge_index`；删除按 `fileId` 双写软删（`product_document` + `knowledge_document`），仅 manager/administrator | 同上 |
| P1-08-06/07 | `POST /products/{id}/analyze`（投 `product_knowledge` 任务，`action=analyze` 仅预览不落库）与 `/knowledge/generate`（`action=generate` 落库 `draft`）；`/knowledge/confirm` 人工确认 `draft→approved`（记 `confirmedBy/confirmedAt`，仅 manager/administrator） | `server/apps/api/src/products/products.service.ts`、`packages/runtime/src/flows.ts` |
| P1-08-08 | 成本隔离：产品上下文仅出 `Overview/Specifications/Pricing(suggestedPrice+priceTiers)/已索引资料`，**`costPrice` 既不进 `productContext` 也不进 prompt**（另叠加 runtime `stripSensitiveFields` 兜底）；`product_knowledge` 提示词并列红线「禁止编造参数/认证/价格」 | `server/packages/workflows/src/flows.ts`（`loadProductContext`）、`prompts.ts`（`product.knowledge`） |
| P1-08-09 | 08 图（新增 `task_type=product_knowledge`，见下方偏差说明）：`load_product_context`（按 `sources` 解析产品结构化数据 + 已索引 chunk）→ `generate_knowledge`（LLM，`outputSchema=productKnowledge`）→ `write_product_knowledge`；内部无高风险节点，全程无审批介入 | `server/packages/workflows/src/sops.ts`、`flows.ts`、`output-schemas.ts` |
| P1-08-10 | 知识生成落 `product_knowledge`：四类条目（advantages/faqs/scenarios/salesScripts）+ `status=draft` + `citations[]`（docId/docName/chunkId 指向具体文档）+ `taskId/generatedAt` 溯源；按 `productId` upsert（重新生成回落 `draft` 并清空确认痕迹） | `server/packages/workflows/src/flows.ts`（`writeProductKnowledge`） |
| P1-08-11 | 归档联动 11 分类「产品」：索引完成后回写 `product_document.indexed=true`（按 `orgId+knowledgeDocId`），前端资料列表轮询至终态 | `server/apps/worker/src/queues/knowledge-index.ts` |
| P1-08-12 | 产品列表页：关键词 + 分类/状态筛选（全量参数进 query key）、缩略图/行内 MOQ 展示、行点击进详情、`+ 添加产品` | `web/src/features/products/views/ProductsListView.vue` |
| P1-08-13 | 产品详情页 5 页签（`?tab=` 同步 URL，刷新/分享保持）；Pricing 页签标注「AI 只读不改价」；资料页签上传态与索引态可见 | `web/src/features/products/views/ProductDetailView.vue`、`components/Product{Documents,Knowledge}Panel.vue` |
| P1-08-14 | 添加/编辑表单（规格 + 阶梯价动态行、必填校验、SKU 重复提示）；AI 分析（弹层 `TaskProgressCard` + `outputs` 预览）/AI 生成（落 draft）/人工确认（draft ↔ approved 差异可见，含 `citations` 展示） | `web/src/features/products/components/ProductFormDialog.vue`、`composables/useProductKnowledge.ts` |
| P1-08-15 | `api/resources/products.ts`（9 函数）+ `api/types/products.ts` + query key + 枚举字典（productStatus/productDocType/productKnowledgeStatus）+ i18n（zh-CN/en 全量键）+ 路由占位替换（`products` / `product-detail` 真实组件） | `web/src/api/{resources,types}/products.ts`、`web/src/utils/enum-map.ts`、`web/src/locales/*`、`web/src/router/routes/modules.ts` |
| 数据层 | `task_type` 新增枚举值 `product_knowledge`：drizzle 枚举 + `0014_manual_task_type_product_knowledge.sql`（`ALTER TYPE … ADD VALUE IF NOT EXISTS`，幂等）+ `TASK_TYPE.PRODUCT_KNOWLEDGE` + 队列映射 `q:knowledge_index`（与 `product_analysis` 同队列，processor 按 `job.data.docId` 是否为空分流） | `server/packages/db/src/schema/enums.ts`、`migrations/manual/0014_*.sql`、`packages/shared/src/enums/index.ts`、`contracts/queues.ts`；`server/apps/api/src/tasks/tasks.dto.ts`（创建/列表 type 白名单） |
| 校验 | 后端 `pnpm typecheck`（16 任务全绿）+ `eslint`；前端 `vue-tsc` + `eslint` + `stylelint` + `vitest run`（13 文件 / 73 例通过，5 个 contract 用例因本地无 API 服务而跳过，与改动无关） | — |

> **实施偏差（08 §P1-08-09）**：任务计划表原文为「`product_analysis` 图」，但该 `task_type` 自 M5-C4 起已被 `customers.analyze` / `leads.batchAnalyze`（客户购买意向分析 SOP）占用。经确认改为**新增 `task_type=product_knowledge`** 承载 08 流程（迁移 `0014` + shared 枚举 + 新 SOP + 提示词/输出契约），语义与验收点不变，且不动客户/线索既有分析链路。前端 08 页面为 P1 路由，受 `features.products` 开关控制（默认 `p0` 隐藏，`p1` 启用）。

## 5. 本轮完成明细（S1-B · 分析预聚合，P1-X-40~43）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-X-40 | `AnalyticsEtl` 扫描器：每小时跨租户扫全部 org，按 `org.timezone` 回算最近 2 个当地日，把业务表区间计数写入 `analytics_daily_summary`；唯一键 `(org_id, stat_date, country, employee_id)` `ON CONFLICT DO UPDATE` 幂等 upsert（重复 tick 不增行）；接入 worker 扫描循环并与 SIGTERM 优雅停机联动 | `server/apps/worker/src/scheduler/analytics-etl.ts`、`apps/worker/src/index.ts`、`packages/core/src/time-window.ts`（`zonedDayRangeUtc` 墙钟日区间，DST 正确） |
| P1-X-41 | 指标口径单点实现：基础指标 `newCustomers/newInquiries/newQuotes` 按业务表 `created_at` 区间计数（与 dashboard KPI 对齐）；AI 贡献按 §7 定义——`foundCustomers`（`customer.source_lead_id`→`ai_lead.task_id`→`ai_task.employee_id`）、`repliedEmails`（`message` out/ai/sent 且 `sent_at` 在区间）、`promotedInquiries`、`savedHours`；物化 `(ALL,ALL)` org 级 + `(ALL,emp)` 员工级 | `server/apps/worker/src/scheduler/analytics-etl.ts` |
| P1-X-42 | `SAVED_HOURS_BASELINE_MINUTES`（获客 10min/客户、邮件 15min/封、跟进 5min/次）、`computeSavedHours`（1 位小数，对齐 `numeric(10,1)`）、`ANALYTICS_CALIBER_NOTE` 口径说明常量、`ANALYTICS_METRIC` 下钻枚举——供数据中心接口（15 §3，S4）随响应返回 | `server/packages/core/src/analytics.ts` |
| P1-X-43 | `PROMOTED_INQUIRY_WINDOW_DAYS=14`、`isWithinAttributionWindow`、`pickLastTouch`（窗口内 `sent_at` 最大者）纯函数 + ETL 中对应 CTE（会话首条来信 + 窗口内最近 AI 外联最后触点 → `customer_activity.operator_id` 归因到 AI 员工） | `server/packages/core/src/analytics.ts`、`server/apps/worker/src/scheduler/analytics-etl.ts` |
| 测试 | core 口径单测 14 例（savedHours 估算、14 天窗口含边界、最后触点、当地日/DST 切分）；worker ETL 集成测试（固定时间窗种子，断言 org 级 + 员工级指标与唯一键幂等） | `server/packages/core/test/analytics.test.ts`、`server/apps/worker/test/analytics-etl.integration.spec.ts` |
| 校验 | `pnpm -w typecheck`（16 任务全绿）、`eslint`、`pnpm --filter @tradepilot/core test`（7 文件 / 77 例全绿） | — |

> 粒度说明：`country` 维度不做日增量物化——15 §3.2「市场分布」是客户**存量**国家分布（非当日新增），由数据中心接口按业务表实时聚合；本表统一以 `'ALL'` 落 `country`，唯一键保留该维度以备扩展。`promotedInquiries` 归因仅计「会话首条来信」且在 14 天窗口内存在 AI 外联者，证据（最后触点）可复算。

## 6. 本轮完成明细（S1-B · 任务中心操作，P1-X-30~33）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-X-30（状态机联动） | `POST /tasks/{id}/pause`、`/resume`、`/cancel`：pause `running/scheduled→paused`（paused 不占并发 → 释放员工位 + 清 delayed job + 推 SSE `status=paused`，执行中任务在下个节点探测离开 running 后中止、检查点保留）；cancel `running/scheduled/paused→canceled` 终态（补 `finished_at` + 释放员工位 + `status`+`done` 收口流，`waiting_approval` 交审核中心不留悬挂）；resume 语义分流——已产生检查点（`started_at` 非空）`paused→running` 并按 `fromPause` 重投 task 队列、从未执行则 `paused→scheduled` 交 Dispatcher 全新投递 | `server/apps/api/src/tasks/tasks.{service,controller}.ts` |
| P1-X-30（检查点续跑） | Runner `run(taskId, { fromPause })`：`claim` 支持 `paused→running` 乐观锁（API 已置 running 时幂等放行）+ 员工 `working` 同步；`graph.invoke(null)` 从最近检查点续跑当前节点（工具按 `taskId+nodeId` 幂等防重复）；worker processor 解析 `job.data.fromPause` | `server/packages/runtime/src/runner.ts`、`server/apps/worker/src/queues/processor.ts` |
| P1-X-31 | `POST /tasks/{id}/transfer-to-human`：`running→paused`（中断图）+ 释放员工位 + `outputs` 追加 `{ type:'handoff', payload:{ fromStatus, reason, summary, assignee, transferredAt, transferredBy } }` 交接摘要（append 语义、可回溯，缺省摘要兜底）；`paused/failed` 仅追加摘要不改状态 | `server/apps/api/src/tasks/tasks.service.ts` |
| P1-X-32 | 启用 04 预留的 `TaskEnqueuer.removeTask`（终态/暂停清理 delayed job）与手动暂停/恢复 API；`enqueueResume`/`enqueueResumeFromPause` 先移除终态历史 job 再按 `jobId=taskId` 重投，避免 BullMQ 幂等 no-op 吞掉恢复 | `server/packages/runtime/src/enqueue.ts`、`server/apps/api/src/tasks/tasks.controller.ts` |
| P1-X-33 | `POST /tasks/batch`（`action ∈ { retry, transfer_to_human }`、`taskIds` 1..50）：逐条复用单任务语义，单条失败不阻断、逐条回传 `{ ok, status, newTaskId, error }` 与 `succeeded/failed` 汇总 | `server/apps/api/src/tasks/tasks.{service,dto}.ts` |
| 校验 | `apps/api` + `apps/worker` typecheck 全绿；runtime 单测 3 文件/26 例全绿；新增 `tasks-ops.integration.spec.ts` 8 例（pause/resume/cancel 状态机 + 检查点分流 + 转人工 + 批量 + SSE）本地临时 `.env.test` 下全绿 | — |

## 7. S1 收口

**S1（横切底座）全部任务已交付**：`P1-16-01/05`、`P1-X-10~15`、`P1-X-20~22`、`P1-X-30~33`、`P1-X-40~43`、`P1-DB-01~04`。下一步进入 S2 业务面（产品 → 报价 → 订单 → 分析/经理）。

> 说明：定价引擎为纯函数，09/10 接入时由调用方从 16 单例读取配置传入。集成测试需 `.env.test`（本地缺失，故 `apps/api`、`apps/worker` 的 `test/*.integration.spec.ts` 通常不跑，与既有测试同环境约束）；`.env.test.example` 要求真实 LLM/Embedding/Search provider 凭据。本轮 `P1-X-30~33` 集成测试以**临时 `.env.test`（占位 provider 凭据 + 本地 PG/Redis）**短跑验证后即删，其余集成测试仍受真实凭据约束无法装配。
>
> S5 衔接：开放 API 的业务面（如 `@Scopes('customers:read')` 标注）随 S5 业务路由接入；当前 `ScopesGuard` 已 fail-closed 就位。分析基座（ETL + 口径常量）已就绪，数据中心 REST 接口（`GET /analytics/*`，含 `caliberNote`）随 15 P1-15-01~06 接入。

## 8. 本轮完成明细（S2 · 09 报价中心，P1-09-01~13）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-09-01 | `GET /quotes`（`status` Tab + `keyword`=编号/客户名 + `customerId`，分页）与 `GET /quotes/summary`（all + 五状态计数）；`POST /quotes` 全字段校验：`quantity < product.moq` 与 `validUntil <= 今天` 均抛 `42201`；编号 `QT-YYYYMMDD-NNN` 按 org×当地日序列生成 | `server/apps/api/src/quotes/quotes.{controller,service,dto}.ts` |
| P1-09-02 | `GET /quotes/{id}` 报价头（结构化字段 + 汇率三元组快照）+ 明细行 + 报价级成本汇总 + 客户身份/评分 + 关联审批；`PUT /quotes/{id}` 仅 `draft` 可改（否则 `40901`），明细按整体替换 | 同上 |
| P1-09-03 | `POST /quotes/{id}/ai-pricing`：复用 core 定价引擎（五项成本 / `computeProfitMarginPct` / `buildPricingReasons`），返回 `suggestedUnitPrice` + `profitMarginPct` + `costBreakdown` + `reasons[]`；**价格与成本全部由引擎计算，不经 LLM** | `packages/core/src/pricing.ts`、`quotes.service.ts` |
| P1-09-04 | `POST /quotes/{id}/submit`：`draft → waiting_approval` + 生成 `approval_request(approvalType='quote', riskLevel='high')` 并回写 `quotation.approval_id`，`q:notify` 通知审批人 | `quotes.service.ts`、`packages/runtime/src/enqueue.ts` |
| P1-09-05 | `POST /quotes/{id}/send`：前置审批须为 `approved/edited_approved`（否则 `40901`），经 org connected mailbox 外发（无可用邮箱 → `40901`）；成功后 `status='sent'` + `sent_at` 并写 `customer_activity` | `quotes.service.ts`、`conversations/send-mail.helper.ts` |
| P1-09-06 | `POST /quotes/{id}/mark-won`：`sent → won` + `won_at`；同事务把潜在客户升级为正式客户（`customer.is_formal=false→true`）并返回 `customerUpgraded`，联动 05 §7 | `quotes.service.ts` |
| P1-09-07 | `GET /quotes/{id}/pdf`：服务端渲染（16 组织抬头 name/country/logo + 报价头 + 明细表 + 合计 + 条款）；零依赖最小 PDF 写入器（ASCII 走 Base-14 Helvetica，CJK 走 Type0/STSong-Light 预定义 CMap），导出动作写 `customer_activity(type=quote)` | `server/apps/api/src/quotes/quote-pdf.ts` |
| P1-09-08（§7-A1） | `lost` 最小闭环：`POST /quotes/{id}/mark-lost`（原因**选填**，`draft/waiting_approval/sent → lost`）+ `POST /quotes/{id}/revive`（`lost → draft`），列表增「已失效」Tab | `quotes.controller.ts`、`QuotesListView.vue` |
| P1-09-09（§7-A2） | `GET /quotes/{id}/negotiation-ladder`：按 16 `discountLadder` 生成「轮次 / 让价幅度 / 建议单价」只读建议，**服务端永不返回底价与剩余底线**；未配置返回空数组 | `quotes.service.ts`、`packages/core/src/pricing.ts`（`buildNegotiationLadder`） |
| P1-09-10 | `QuotesListView.vue`：all + 五状态 Tab（每档带数量，`summary` 取数）、关键词/客户筛选（全量参数进 query key）、行点击进详情、`+ 新建报价` | `web/src/features/quotes/views/QuotesListView.vue` |
| P1-09-11 | `QuoteDetailView.vue` + `QuotePricingPanel.vue`：报价头/明细/成本构成/关联审批只读展示；draft 进入即拉取定价建议并以 `InsightCard` **展示推荐原因**（confidence=1 表示确定性核算）；状态机动作按服务端返回状态刷新（提交审核 / 发送[审批未过时禁用+说明] / 成交 / 失效[原因选填] / 复活 / 导出 PDF）；议价梯度未配置时整块隐藏 | `web/src/features/quotes/views/QuoteDetailView.vue`、`components/QuotePricingPanel.vue` |
| P1-09-12 | `QuoteFormDialog.vue`：客户/联系人/币种/贸易术语/有效期/付款方式/汇率 + 明细行动态增删，行内 MOQ 提示与提交前校验（MOQ、正数、有效期须晚于今天），编辑仅 draft | `web/src/features/quotes/components/QuoteFormDialog.vue` |
| P1-09-13 | `api/resources/quotes.ts`（12 函数，含 PDF blob 下载）+ `api/types/quotes.ts` + `qk.quotes.{all,list,detail,summary,ladder}` + 枚举字典 `quoteStatus`/`quoteLostReason` + zh-CN/en `quotes` 命名空间 + `quotes`/`quote-detail` 路由由占位换真实组件 | `web/src/api/{resources,types}/quotes.ts`、`web/src/query/keys.ts`、`web/src/utils/enum-map.ts`、`web/src/locales/*`、`web/src/router/routes/modules.ts` |
| 校验 | 后端 `tsc -p apps/api` + `eslint apps/api/src/quotes` 全绿；前端 `vue-tsc --build --force` + `eslint` + `stylelint` + `vitest run`（13 文件 / 73 例通过；5 个 contract 用例因本地无 API 服务失败，与改动无关） | — |

> **实施说明（09 §3.4）**：`send` 的邮箱通道复用 P0 既有的 `sendMail` 适配器（org connected mailbox），与「邮件外发」链路同源；**不虚构 `SENT` 之外的投递状态**——`status='sent'` 即表示已提交 SMTP 成功。
>
> **实施说明（09 §3.7/A1）**：`quotation` 表无 `lost_at` 列（ER 07），`lost` 时点以 `updated_at` 回显；`lostReason` 存 `lost_reason` 列。
>
> **实施说明（09 §3.8/A2）**：议价梯度为**纯计算**接口（不落库、不写活动），避免"看一眼建议"产生副作用；底价（`profitFloor`）仅用于服务端红线校验，任何响应均不下发。

## 9. 本轮完成明细（S2 · 10 订单中心，P1-10-01~16）

| 任务 | 交付物 | 证据 |
|---|---|---|
| P1-10-01 | `GET /orders`（`tab`/`status` + `customerId` + `risk` 筛选，分页，返回 `counts`（all + 四状态）与 `riskCounts`）与 `POST /orders`（`fromQuoteId` 转单须报价 `won`，否则 `42201`；手工建单要求 `customerId` + `items`）；编号 `ORD-YYYYMMDD-NNN` 按 org×当地日序列生成 | `server/apps/api/src/orders/orders.{controller,service,dto}.ts` |
| P1-10-02 | `GET /orders/{id}`：订单头（客户/联系人/负责人/交期/付款方式/来源报价）+ 进度四要素 + 明细行 + 最近一次风险洞察 + 在途 `order_change` 审批 + 进度流水 `timeline` | 同上 |
| P1-10-03 | `PUT /orders/{id}`（交期/金额/明细）：**不直接落库**，生成 `approval_request(approvalType='order_change', riskLevel='high', bizType='sales_order')`，`q:notify` 通知审批人；AI 身份提交抛 `40301`；已有在途变更审批抛 `40901` | `orders.service.ts`、`approvals/approvals.service.ts`（`applyOrderChange`） |
| P1-10-04 | `PUT /orders/{id}/progress`（**唯一状态录入入口**）：部分更新四要素 + 写 `order_progress_log`（备注由服务端按动作生成，避免前端拼文案） | `orders.service.ts` |
| P1-10-05 | `GET /orders/{id}/risk`：规则引擎实时评估（`plannedPct` 按 `linear_by_time` 线性推导 + `thresholdPct` 阈值判定），响应必带 `evidence`{`plannedPct`,`actualPct`,`planSource`,`thresholdPct`}；缺证据一律 `normal` | `packages/core/src/order-risk.ts`、`orders.service.ts` |
| P1-10-06 | `POST /orders/{id}/risk/execute`：`suggestionIds` 分流 —— `internal → taskIds`（建 `follow_up_task` 并投 `order_monitor` 图）、`customer → approvalIds + draft`（会话草稿 + 审批） | `orders.service.ts` |
| P1-10-07 | `POST /orders/{id}/draft-email`：联动 06 生成延期沟通草稿（写入会话 `message`），**仅生成草稿 + 审批，绝不自动外发** | `orders.service.ts`、`conversations/` |
| P1-10-08 | 转单 `cost_snapshot` 原样复制（不重算，保证报价/订单成本口径一致）；手工建单缺省 `{}` 不阻塞 | `orders.service.ts` |
| P1-10-09（**A3**） | `progress → status` 服务端自动推导（`pending_payment → in_production → ready_to_ship → completed`），状态对前端只读 | `packages/core/src/order-risk.ts`（`deriveOrderStatus`） |
| P1-10-10（**A4**） | `planSource` 统一 `linear_by_time`（详情/评估/图三处同口径） | 同上 |
| P1-10-11 | `order_monitor` 图：`load_order`（订单快照 + 越权/不存在落 error 日志并收尾）→ `assess_risk`（规则引擎）→ `alert_anomaly`（异常告警 `ai_task_log` + `outputs.insight`）；注册进 SOP 表与 `WORKFLOW_SOP_DEFINITIONS` | `packages/workflows/src/{flows,sops,outputs}.ts` |
| P1-10-12 | 进度更新后自动评估履约风险 → `order_risk_insight` 幂等写回（active 复用/更新，`normal` 则 `resolved`），并同步 `sales_order.risk` 供列表徽标取数 | `orders.service.ts`、`flows.ts` |
| P1-10-13 | `OrdersListView.vue`：all + 四状态 Tab（每档带 `summary` 数量）+ 履约风险筛选（全量参数进 query key）+ 行点击进详情 + `+ 新建订单`；状态/风险徽标取字典语义色（`at_risk` 红色） | `web/src/features/orders/views/OrdersListView.vue` |
| P1-10-14 | `OrderFormDialog.vue`：**转单**（筛选已成交报价，明细/客户由服务端快照复制）与**手工建单**（客户 + 明细动态增删 + 行级校验 + 币种）双模式；同一弹窗以 `mode='change'` 承载变更提交（含「须审批」提示，明细变更由服务端重算总额） | `web/src/features/orders/components/OrderFormDialog.vue` |
| P1-10-15 | `OrderDetailView.vue` + `OrderProgressPanel.vue` + `OrderRiskPanel.vue`：进度四要素 Steps + 更新弹窗 + 进度流水时间线；风险面板块展示判定原因 / **证据可下钻**（计划进度、实际进度、口径、评估来源与时间）/ AI 建议勾选执行 / 一键生成延期沟通草稿；规则引擎（`confidence=null`）不呈现置信度，仅在 AI 洞察覆盖文案时经 `InsightCard` 呈现 | `web/src/features/orders/views/OrderDetailView.vue`、`components/{OrderProgressPanel,OrderRiskPanel}.vue` |
| P1-10-16 | `api/resources/orders.ts`（8 函数）+ `api/types/orders.ts` + `qk.orders.{all,list,detail,summary}` + 枚举字典 `orderStatus`/`orderRisk`/`orderSuggestionType` + zh-CN/en `orders` 命名空间 + `orders`/`order-detail` 路由由占位换真实组件 | `web/src/api/{resources,types}/orders.ts`、`web/src/query/keys.ts`、`web/src/utils/enum-map.ts`、`web/src/locales/*`、`web/src/router/routes/modules.ts` |
| 校验 | 后端 `tsc -p apps/api`（含 core/workflows/worker）与 `eslint apps/api/src/orders packages/core/src/order-risk.ts` 全绿；前端 `vue-tsc --build` + `eslint` + `stylelint` 全绿 | — |

> **实施说明（10 §4 AI 边界）**：金额 / 交期 / 数量变更**一律**经 `order_change` 高危审批（服务端仅返回 `approvalId`，前端不落库）；风险执行中面向客户的动作为「生成草稿 + 审批」，外发由审批中心 `email_send` 分支驱动（`bizType='message'`，批准后 mailbox 真实外发该草稿），与 P0 `message_send` 口径合并为同一闭环。
>
> **实施说明（10 A3/A4/A5）**：状态、计划进度口径、落后阈值全部由服务端确定性推导（规则引擎，非 LLM 生成），前端只做展示与四要素录入；风险判定结果写入 `order_risk_insight` 以满足详情/列表/图三处一致。
>
> **实施说明（路由）**：`orders/:id` 与 `products/:id`、`quotes/:id` 统一改为**平铺详情路由**（不进菜单），因列表视图容器内不含内层 `router-view`，原「列表路由 children 嵌详情」写法会导致详情页不渲染（同 04 `customers/:id` 既有写法）。


